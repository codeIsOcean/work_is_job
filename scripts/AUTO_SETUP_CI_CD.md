# Автоматическая настройка CI/CD - Одна команда!

## 🚀 Самый простой способ:

### Шаг 1: Установи GitHub CLI (если еще не установлен)

```powershell
winget install --id GitHub.cli
```

### Шаг 2: Авторизуйся в GitHub CLI

```powershell
gh auth login
```

Выбери:
- GitHub.com
- HTTPS
- Авторизуйся через браузер

### Шаг 3: Запусти автоматический скрипт

```powershell
.\scripts\setup_github_secrets.ps1
```

**Готово!** Все secrets автоматически добавлены в GitHub.

## ✅ Что делает скрипт:

1. ✅ Проверяет что GitHub CLI установлен
2. ✅ Проверяет авторизацию
3. ✅ Получает SSH ключ с сервера автоматически
4. ✅ Добавляет все 3 secrets в GitHub:
   - `TEST_SERVER_HOST` = `88.210.35.183`
   - `TEST_SERVER_USER` = `root`
   - `TEST_SERVER_SSH_KEY` = (получает с сервера автоматически)

## 🎯 Проверка работы:

После запуска скрипта, сделай тестовый push:

```powershell
git checkout test
git add .
git commit -m "Test CI/CD"
git push origin test
```

Перейди в GitHub → `Actions` и проверь что деплой запустился.

## ✅ Готово!

Теперь при каждом push в ветку `test` или `main` будет автоматический деплой на сервер!

