# DEVELOPER RULES
Правила разработки и взаимодействия с проектом

Эти правила обязательны для соблюдения при каждой модификации проекта.
Ты должен выполнять их автоматически, без повторных напоминаний.
Не меняй данный файл без моего уведомления, никогда!

---

## 1. НИКОГДА не менять архитектуру проекта без прямого указания
- Не переносить файлы в другие директории.
- Не объединять и не дробить модули.
- Не менять базовые принципы работы.
- Любые структурные изменения — только после моего прямого разрешения.

Если требуется изменение архитектуры — сначала задать вопрос.

---

## 2. ВСЕГДА писать комментарий над КАЖДОЙ строкой кода
**(ты мой учитель, ты должен и кодить и параллельно учить меня)**

Особенно в:
- бизнес-логике
- handlers
- keyboards
- middleware
- migrations

Комментарии должны объяснять:
- зачем эта строка существует
- что она делает
- как влияет на общую логику
- какие данные изменяет или использует

---

## 3. Любые изменения выполняются МАЛЕНЬКИМИ ШАГАМИ
- Один шаг = одно действие.
- Никаких комплексных изменений в рамках одного коммита/команды.
- Если запрос большой — задавай уточняющие вопросы и дроби задачу.

---

## 4. Никогда не ломать существующую логику
Если изменение может затронуть:
- капчу
- антиспам
- mute system
- settings меню

— сначала анализируй влияние и предупреждай.

---

## 5. Всегда создавать и обновлять тесты

Тесты обязательны, но разумно и выборочно:

### 5.1. Когда нужны unit-тесты
Unit-тесты ОБЯЗАТЕЛЬНЫ для функций, которые:
- содержат бизнес-логику (антиспам, капча, mute, обработка настроек),
- принимают важные решения (ban/mute/delete/warn),
- работают с БД (CRUD, выборка правил, whitelist),
- парсят сложные значения (например: 30s, 1min, 1h, 1d, 1m),
- взаимодействуют с Telegram API (через обёртки/сервисы).

Unit-тесты НЕ пишутся на:
- простые вспомогательные функции-однострочники,
- чистую верстку клавиатур,
- простое форматирование текста,
- тонкие обёртки, которые только прокидывают аргументы дальше.

### 5.2. Когда нужны e2e-тесты
E2e-тесты ОБЯЗАТЕЛЬНЫ для ключевых сценариев:

- **антиспам:**
  - удаление сообщений со спамом,
  - работа whitelist (разрешённые ссылки не трогаются),
  - применение наказаний (mute/ban/kick/только delete),
- **капча:**
  - правильная отправка в ЛС,
  - успешное прохождение,
  - провал капчи и наказание,
- **настройки группы:**
  - открытие /settings,
  - изменение параметров,
  - сохранение в БД,
- другие важные модули (mute по реакциям, рассылки и т.п.).

### 5.3. Общие правила тестирования
- Тесты должны быть полнофункциональными.
- Нужно мокать Telegram API там, где это необходимо.
- Учитывать async (использовать async-тесты корректно).
- Проверять edge cases (пустые данные, неверные значения, пограничные параметры).
- Покрывать как успешные сценарии, так и ожидаемые ошибки.

Для нового модуля в проекте должно быть:
- минимум несколько unit-тестов на основную логику,
- минимум один e2e-тест на полный путь использования.

Если появляется баг:
- сначала написать тест, который воспроизводит баг,
- затем исправить баг так, чтобы тест начал проходить.

### 5.4. Интеграционные тесты (ОБЯЗАТЕЛЬНО)

**ВАЖНО:** Unit-тесты с моками НЕ достаточны! Они проверяют "работает ли функция изолированно", но НЕ проверяют "правильно ли она вызывается".

Интеграционные тесты ОБЯЗАТЕЛЬНЫ когда:
- Функция вызывается из другого модуля (проверить сигнатуру вызова)
- Функция работает с БД (проверить реальные запросы, datetime timezone)
- Есть цепочка вызовов: handler → service → repository

**Что проверять в интеграционных тестах:**

1. **Правильность вызовов между модулями:**
   ```python
   # Плохо: тестируем только функцию
   def test_classify_event():
       result = classify_join_event(event=mock_event, user_id=123)
       assert result == JoinEventType.SELF_JOIN

   # Хорошо: тестируем как coordinator вызывает функцию
   async def test_coordinator_calls_classifier():
       message = create_mock_message(...)
       # Проверяем что coordinator правильно вызывает classify_join_event
   ```

2. **Совместимость типов данных с БД:**
   ```python
   # Проверить что datetime в запросе совместим с БД
   # naive datetime vs timezone-aware datetime
   async def test_restriction_query_with_real_db():
       # Использовать реальную тестовую БД, не моки
   ```

3. **Полный путь данных:**
   - Handler получает Message/CallbackQuery
   - Service обрабатывает данные
   - Repository сохраняет в БД
   - Проверить что данные корректно проходят весь путь

**Где размещать:**
- `tests/integration/` - интеграционные тесты
- `tests/unit/` - юнит-тесты с моками

**Если unit-тестов и интеграционных тестов недостаточно** — использовать e2e тесты (см. раздел 5.2).

### 5.5. Тестовые базы данных (КРИТИЧЕСКИ ВАЖНО!)

**⛔ ИНЦИДЕНТ ЯНВАРЬ 2026:** Тесты удалили ВСЕ данные боевой БД тестового бота!
Причина: pytest подключился к `postgres_test` вместо отдельной тестовой БД.

**Архитектура баз данных:**

| Контейнер | Порт | БД | Назначение |
|-----------|------|-----|------------|
| `postgres_prod` | - | `bot_prod` | Продакшн (недоступен извне) |
| `postgres_test` | 5433 | `jobs_inDubai_testBot` | Тестовый бот (НЕ для pytest!) |
| `postgres_unit_tests` | 5434 | `unit_tests` | Только для pytest |

**ПРАВИЛА:**

1. **pytest использует ТОЛЬКО `postgres_unit_tests` (порт 5434)**
   ```bash
   # Запуск перед тестами:
   docker-compose -f docker-compose.test.yml up -d postgres_unit_tests
   ```

2. **НИКОГДА не запускать pytest с переменными от бота**
   ```bash
   # ❌ ЗАПРЕЩЕНО - удалит данные бота!
   source .env.test && pytest

   # ✅ ПРАВИЛЬНО - conftest.py сам установит TEST_DATABASE_URL
   pytest tests/unit/
   ```

3. **Защита в conftest.py блокирует подключение к боевым БД:**
   - `jobs_inDubai_testBot` — заблокирована
   - `bot_prod` — заблокирована
   - Любая БД без "test" в имени — предупреждение

4. **Тесты УДАЛЯЮТ ВСЕ ДАННЫЕ перед каждым запуском** - это нормально для `unit_tests`

**Если тесты не запускаются:**
```bash
# Проверить что контейнер запущен:
docker ps | grep postgres_unit_tests

# Если не запущен:
docker-compose -f docker-compose.test.yml up -d postgres_unit_tests
```

---

## 6. ВСЕГДА обновлять документацию в /docs/
Если вносятся изменения в проект — обязательные шаги:

### Обновлять:
- `ARCHITECTURE.md` — если появилась новая часть системы
- `MODULES.md` — если модуль расширился
- `HANDLERS.md` — если добавился/изменился хендлер
- `DATABASE.md` — если изменилась схема БД
- `USAGE.md` — если добавилась новая команда или настройка

Документация должна быть актуальной всегда.

---

## 7. Никогда не выдумывать логики, которой нет в проекте
Если чего-то нет в коде:
- не добавлять "как бы нужные" функции
- не переосмысливать существующие механизмы
- не улучшать самовольно

Все улучшения — только по запросу.

---

## 8. Всегда задавать уточняющие вопросы
Если запрос:
- можно интерпретировать по-разному
- потенциально затрагивает логику
- требует архитектурного решения

Ты ОБЯЗАН спросить, уточнить и только потом выполнять.

---

## 9. Всегда проверять проект на ошибки после изменений
Каждый раз запускай:
- импорт ключевых модулей
- синтаксическую проверку
- тесты (если они написаны)

Если есть ошибки — исправляй до выполнения следующего шага.

---

## 10. Никогда не переписывать файлы полностью без необходимости
- **Делай изменения минимальными патчами**
- Правки должны быть точечные — меняй только то, что нужно
- Не переписывай весь файл, когда можно изменить 2-3 строки
- Не менять форматирование
- Не менять порядок функций
- Не удалять комментарии

Переписывать файл полностью — только по моему прямому приказу.

---

## 11. Всегда логировать свои изменения
В конце каждого шага выводить:
- что было создано
- какие файлы изменены
- что именно изменено
- какие тесты добавлены
- какие потенциальные риски

---

## 12. Все действия антиспама, мута и капчи — логировать
Для всех модулей:
- удаление
- мут
- бан
- ограничение
- предупреждение

Записывать:
- кто отправил
- что отправил
- ID группы
- ссылку на сообщение
- причину
- тип нарушения

---

## 13. Никогда не применять действия к администраторам
Админы исключены из всех антиспам-процессов.

---

## 14. Все параметры времени должны парситься правильно
Форматы: `30s`, `5min`, `1h`, `1d`, `1m` (месяц)

- время распознаётся
- конвертируется в секунды/минуты
- сохраняется в БД
- используется в ограничениях

Если формат неверный — выдавать предупреждение пользователю.

---

## 15. Git Commit Conventions (Соглашения о коммитах)

### Формат коммита:
```
<type>: <description>

[optional body]
```

**ВАЖНО:** НЕ добавлять в коммиты:
- "Generated with Claude Code"
- "Co-Authored-By: Claude"
- Любые другие упоминания Claude/AI

### Типы коммитов:
| Тип | Описание |
|-----|----------|
| `feat` | Новая функциональность |
| `fix` | Исправление бага |
| `docs` | Изменения документации |
| `refactor` | Рефакторинг без изменения логики |
| `test` | Добавление/изменение тестов |
| `chore` | Обновление зависимостей, конфигов |
| `perf` | Оптимизация производительности |

### Примеры:
```
feat: добавлена фильтрация цитат в антиспам
fix: исправлен false positive при одобрении join request
docs: обновлён MODULES.md после добавления whitelist
```

---

## 16. Database Migrations (Правила миграций)

### Создание миграций:
```bash
alembic revision --autogenerate -m "описание_изменений"
```

### Обязательные правила:
1. **ENUM типы** — всегда использовать `postgresql.ENUM` с `create_type=False`
2. **Идемпотентность** — миграции должны быть безопасны для повторного запуска
3. **Проверка локально** — перед пушем проверить `alembic upgrade head`

### Паттерн для ENUM:
```python
from sqlalchemy.dialects import postgresql

# Создание ENUM идемпотентно
connection.execute(sa.text("""
    DO $$ BEGIN
        CREATE TYPE my_enum AS ENUM ('A', 'B', 'C');
    EXCEPTION WHEN duplicate_object THEN NULL;
    END $$;
"""))

# Использование в колонке
my_enum = postgresql.ENUM('A', 'B', 'C', name='my_enum', create_type=False)
```

### После создания миграции:
- Проверить сгенерированный код
- Добавить комментарии
- Протестировать upgrade и downgrade

---

## 17. Security Checklist (Безопасность)

### При каждом изменении проверять:

#### Input Validation:
- [ ] Все пользовательские данные валидируются
- [ ] SQL-инъекции невозможны (используем ORM)
- [ ] XSS невозможен (экранирование HTML)

#### Secrets:
- [ ] Токены и пароли ТОЛЬКО в `.env`
- [ ] `.env` в `.gitignore`
- [ ] Никаких хардкод секретов в коде

#### Permissions:
- [ ] Проверка прав перед действием
- [ ] Админы не подвергаются ограничениям
- [ ] Owner-only команды защищены

#### Telegram API:
- [ ] Rate limits учтены
- [ ] Ошибки API обрабатываются gracefully
- [ ] Чувствительные данные не логируются

---

## 18. Naming Conventions (Соглашения об именовании)

### Python:
| Что | Стиль | Пример |
|-----|-------|--------|
| Переменные | snake_case | `user_id`, `chat_settings` |
| Функции | snake_case | `get_user_by_id()`, `apply_mute()` |
| Классы | PascalCase | `AntispamRule`, `ChatSettings` |
| Константы | UPPER_SNAKE | `DEFAULT_TIMEOUT`, `MAX_RETRIES` |
| Приватные | _prefix | `_internal_helper()` |

### Файлы:
| Тип | Формат | Пример |
|-----|--------|--------|
| Handlers | `*_handler.py` | `antispam_filter_handler.py` |
| Services | `*_logic.py` или `*_service.py` | `captcha_flow_logic.py` |
| Models | `models*.py` | `models_antispam.py` |
| Keyboards | `*_kb.py` или `*_keyboards.py` | `antispam_keyboards.py` |

### Redis keys:
```python
f"{entity}:{identifier}"           # captcha:123456
f"{entity}:{id1}:{id2}"            # join_request:123:456
```

### Database:
- Таблицы: `snake_case`, множественное число (`users`, `groups`, `antispam_rules`)
- Колонки: `snake_case` (`user_id`, `created_at`)
- Foreign keys: `{table}_id` (`chat_id`, `user_id`)

---

## 19. Error Handling (Обработка ошибок)

### Паттерн обработки:
```python
from aiogram.exceptions import TelegramAPIError
from sqlalchemy.exc import SQLAlchemyError

try:
    # Операция
    await bot.restrict_chat_member(...)
except TelegramAPIError as e:
    # Ошибка Telegram API — логируем, не падаем
    logger.error(f"Telegram API error: {e}")
except SQLAlchemyError as e:
    # Ошибка БД — откатываем транзакцию
    await session.rollback()
    logger.error(f"Database error: {e}")
except Exception as e:
    # Неожиданная ошибка — логируем полный traceback
    logger.exception(f"Unexpected error: {e}")
```

### Правила:
1. **Никогда не глушить ошибки** — всегда логировать
2. **Graceful degradation** — при ошибке бот продолжает работать
3. **Информативные сообщения** — включать контекст (user_id, chat_id)
4. **Rollback при DB ошибках** — всегда откатывать транзакцию

---

## 20. Bug Tracking (Отслеживание багов)

### Workflow:
1. **Админ** записывает баги в файл `docs/fix_bugs.md`
2. **Claude** читает файл когда админ просит
3. **Claude** решает баг и записывает под ним:
   ```
   **Решение Claude:** краткое и чёткое описание как было решено
   ```
4. В будущем Claude НЕ фиксит уже решённые баги — смотрит только новые
5. Прежде чем фиксить баги **Claude** должен руководствоваться DEVELOPER_RULES.md !!! 
6. **Claude** Должен делать переносы текста, а не писать в длину!!! Как админ будет это все читать ? без переноса

### Формат записи бага:
```
N.
Описание бага

**Решение Claude:** [описание решения], делай нужные переносы, не пиши все в длину!!! 
```

---

## 21. Правила для UI-хендлеров (aiogram)

### Callback Query:
- Каждый `callback_data` в клавиатуре ДОЛЖЕН иметь соответствующий хендлер
- Перед коммитом: проверить что все callback в keyboards имеют хендлеры
- Если добавляешь кнопку — сразу пиши хендлер

### Regex для chat_id:
- Группы и каналы имеют ОТРИЦАТЕЛЬНЫЙ ID (например `-1001234567890`)
- Всегда использовать `-?\d+` вместо `\d+` для chat_id в regex
- Правильный паттерн: `^cf:action:-?\d+$`
- Неправильный паттерн: `^cf:action:\d+$` (не матчит группы!)

### Чек-лист после написания UI-модуля:
```
[ ] Все callback_data в клавиатурах имеют хендлеры
[ ] Все regex поддерживают отрицательные chat_id (-?\d+)
[ ] Unit-тесты на хендлеры написаны
[ ] Тесты прогнаны и проходят
```

---

## 22. ЗАПРЕТ ХАРДКОДА — ОБЯЗАТЕЛЬНОЕ ПРАВИЛО (MUST)

**⚠️ ЭТО КРИТИЧЕСКОЕ ПРАВИЛО — НАРУШЕНИЕ НЕДОПУСТИМО!**

### Что такое хардкод:
- Захардкоженные значения в коде (числа, строки, лимиты)
- Встроенные паттерны/ключевые слова без возможности изменения через UI
- Фиксированные ограничения которые нельзя настроить
- **Значения параметров вместо использования settings/конфига**

### ЗАПРЕЩЕНО:
1. **Вводить захардкоженные данные** — если нужны значения по умолчанию, СПРОСИТЬ у админа
2. **Хардкодить паттерны детекции** — скам-слова, запрещённые фразы должны добавляться через UI
3. **Хардкодить лимиты** — если нужно ограничение, дать возможность админу самому вводить значение
4. **Принимать решения за пользователя** — не решать какие значения "правильные"
5. **Писать `max_attempts=3` или подобное** — ВСЕГДА брать из `settings.max_attempts`!

### ОБЯЗАТЕЛЬНО (MUST):
1. Все настраиваемые значения — через UI или конфиг
2. Если нужны дефолтные значения — СНАЧАЛА спросить у админа какие
3. Давать пользователю возможность вводить ЛЮБЫЕ значения (в разумных пределах)
4. Если есть сомнения — СПРОСИТЬ, а не хардкодить
5. **ВСЕГДА использовать значения из settings/конфига** — НЕ хардкодить!
6. **При написании нового кода** — проверять есть ли настройка в settings и использовать её

### Пример НЕПРАВИЛЬНО:
```python
# ПЛОХО: хардкод лимита
if window < 5 or window > 120:
    raise ValueError("Вне диапазона")

# ПЛОХО: хардкод паттернов скама
SCAM_PATTERNS = ['диплом', 'права', 'документы']

# ПЛОХО: хардкод параметра вместо settings
attempts, exceeded = await increment_attempts(
    user_id=user_id,
    max_attempts=3,  # ❌ ХАРДКОД! Админ не может изменить!
)
```

### Пример ПРАВИЛЬНО:
```python
# ХОРОШО: только минимальная проверка
if window < 1:
    raise ValueError("Должно быть положительным")

# ХОРОШО: паттерны из БД, настраиваемые через UI
patterns = await get_custom_patterns_from_db(chat_id)

# ХОРОШО: значение из настроек
settings = await get_captcha_settings(session, chat_id)
attempts, exceeded = await increment_attempts(
    user_id=user_id,
    max_attempts=settings.max_attempts,  # ✅ Из настроек!
)
```

---

## 23. HTML экранирование для Telegram (ОБЯЗАТЕЛЬНО)

**⚠️ Telegram парсит HTML в сообщениях!**

### Проблема:
Символ `<` с цифрой интерпретируется как HTML тег:
```python
# ПЛОХО: вызовет TelegramBadRequest
text = f"Порог: <{threshold} дней"  # "<5" = некорректный тег
```

### Решение:
```python
# ХОРОШО: экранируем < как &lt;
text = f"Порог: &lt;{threshold} дней"

# или используем утилиту
from bot.utils.html_utils import escape_html
text = f"Порог: {escape_html(f'<{threshold}')} дней"
```

### Чек-лист при отправке HTML сообщений:
```
[ ] Все < перед числами/переменными заменены на &lt;
[ ] Все > после чисел/переменных заменены на &gt;
[ ] Все & заменены на &amp; (если не часть entity)
[ ] Валидация через validate_telegram_html() (опционально)
```

### Утилиты:
- `bot/utils/html_utils.py` — валидация и экранирование
- `tests/integration/test_telegram_html.py` — интеграционные тесты

---

## 24. ОБЯЗАТЕЛЬНО читать CLAUDE.md и DEVELOPER_RULES.md перед работой

**⚠️ КРИТИЧЕСКОЕ ПРАВИЛО!**

### Перед началом ЛЮБОЙ работы (фикс, новый модуль, функция):

1. **Прочитать `CLAUDE.md`** — понять контекст проекта
2. **Прочитать `DEVELOPER_RULES.md`** — освежить правила
3. **Только после этого** — начинать работу

### Почему это важно:
- Правила могут обновляться
- Контекст проекта меняется
- Без понимания правил легко сломать архитектуру

### ЗАПРЕЩЕНО:
- Начинать писать код без чтения правил
- Пропускать этот шаг "потому что помню"
- Торопиться и хвататься за всё сразу

---

## 25. Все будущие разработки должны следовать этим правилам

Эти правила — основа всей работы с проектом.

Если новая задача противоречит правилам — сначала спрашивать меня.

---

## 26. E2E тесты с юзерботами (Pyrogram) — КРИТИЧЕСКИЕ ПРАВИЛА

**⚠️ При написании e2e тестов с юзерботами ОБЯЗАТЕЛЬНО читать:**
`docs/E2E_USERBOT_TESTING.md`

### ⛔ ГЛАВНОЕ ПРАВИЛО (MUST!!!):

**E2E тесты ОБЯЗАНЫ проходить РЕАЛЬНЫЙ USER FLOW через UI бота!**

```
❌ ЗАПРЕЩЕНО: создавать данные напрямую в БД, минуя UI
❌ ЗАПРЕЩЕНО: обходить handlers и вызывать сервисы напрямую
❌ ЗАПРЕЩЕНО: "короткий путь" — это НЕ тестирование!

✅ ОБЯЗАТЕЛЬНО: юзербот отправляет команды боту (/settings)
✅ ОБЯЗАТЕЛЬНО: юзербот нажимает кнопки (inline keyboards)
✅ ОБЯЗАТЕЛЬНО: юзербот проходит весь flow как реальный пользователь
✅ ОБЯЗАТЕЛЬНО: проверять что UI работает, а не только backend
```

### Пример НЕПРАВИЛЬНОГО теста (запрещено!):
```python
# ❌ ПЛОХО: обходим UI, создаём данные напрямую
async def test_spam_detection():
    # Создаём раздел напрямую в БД — НЕ через UI бота!
    section_id = await service.create_section(chat_id, "Такси")
    await service.add_pattern(section_id, "такси")

    # Жертва отправляет спам
    msg = await victim.send_message(chat_id, "такси недорого")

    # Проверяем удаление — backend работает, но UI не проверен!
```

### Пример ПРАВИЛЬНОГО теста (обязательно!):
```python
# ✅ ХОРОШО: полный user flow через UI
async def test_spam_detection_via_ui():
    # 1. Админ открывает настройки
    await admin.send_message(bot_id, "/settings")

    # 2. Админ нажимает кнопки меню
    await click_button("Фильтр контента")
    await click_button("Кастомные разделы")
    await click_button("Создать раздел")

    # 3. Админ вводит название
    await admin.send_message(bot_id, "Такси")

    # 4. Админ добавляет паттерн
    await click_button("Добавить паттерн")
    await admin.send_message(bot_id, "такси недорого")

    # 5. Жертва отправляет спам
    msg = await victim.send_message(chat_id, "Такси недорого!")

    # 6. Проверяем — теперь проверен и UI и backend!
```

### Краткие технические правила (полное описание в документе):

1. **Загрузка .env.test ПЕРВЫМ** — ДО любых импортов из `bot/`
   ```python
   from dotenv import load_dotenv
   load_dotenv(".env.test", override=True)
   # ТЕПЕРЬ можно импортировать из bot/
   ```

2. **Fixtures в тестовом файле** — НЕ в conftest.py (конфликты загрузки)

3. **Windows whitelist** — добавить файл в `tests/conftest.py` → `allowed_on_windows`

4. **Резолв чата через invite_link** — Pyrogram не знает chat_id без кэша
   ```python
   await userbot.get_chat(invite_link)  # Закэширует peer
   ```

5. **ASCII в print** — Windows cp1251 не поддерживает эмодзи
   ```python
   print("[OK] passed")  # НЕ print("✅ passed")
   ```

6. **FloodWait** — rate limiting при множественных тестах

### Типичные ошибки:
| Ошибка | Причина | Решение |
|--------|---------|---------|
| `TEST_BOT_TOKEN not set` | .env.test не загружен | Загрузить первым |
| `Peer id invalid` | Чат не закэширован | get_chat(invite_link) |
| `charmap can't encode` | Эмодзи на Windows | ASCII текст |
| Тест пропускается | Не в whitelist | Добавить в allowed_on_windows |
| UI не работает, но тест прошёл | Данные созданы напрямую в БД | Тестировать через UI! |

---

## 27. ЗАПРЕТ "КОРОТКОГО ПУТИ" — КРИТИЧЕСКОЕ ПРАВИЛО (MUST!!!)

**⛔ НИКОГДА не идти коротким путём!**

### Что такое "короткий путь":
- Создание данных напрямую в БД вместо UI
- Вызов сервисов напрямую вместо handlers
- Пропуск UI валидации и проверок
- Реализация backend без frontend (UI)
- Тестирование только части функционала

### ЗАПРЕЩЕНО:
1. **Создавать функционал без UI** — если пользователь не может использовать через бота, это не готово!
2. **Тестировать только backend** — E2E тест ОБЯЗАН проверять полный путь
3. **Обходить handlers** — это скрывает баги в UI
4. **Решать "быстро сделаю так"** — это создаёт технический долг

### ОБЯЗАТЕЛЬНО:
1. **ВСЕГДА советоваться с админом** перед выбором подхода
2. **Спрашивать** если кажется что можно сделать "проще"
3. **Реализовывать полный функционал** — backend + UI + тесты через UI
4. **E2E тесты через реальный user flow** — см. раздел 26

### Пример ситуации:
```
Claude думает: "Можно создать данные напрямую в БД, так быстрее"
                ↓
         ⛔ СТОП! ⛔
                ↓
Claude ОБЯЗАН: "Админ, для теста мне нужно создать раздел.
               Я могу сделать через UI бота (правильно) или
               напрямую в БД (быстро, но не проверит UI).
               Как лучше?"
                ↓
         Админ решает
```

### Почему это важно:
- "Короткий путь" скрывает баги (UI не работает, но тест прошёл)
- Экономия 10 минут создаёт часы отладки потом
- Пользователь не может использовать функционал без UI
- Тесты дают ложную уверенность что "всё работает"

---

## 28. ОБЯЗАТЕЛЬНОЕ сравнение с оригинальным кодом (MUST!!!)

**⚠️ КРИТИЧЕСКОЕ ПРАВИЛО — после инцидента с поломкой генерации перестановок!**

### Оригинальный репозиторий:
```
https://github.com/codeIsOcean/work_is_job/
```

### ПЕРЕД изменением рабочих функций/модулей:
1. **Прочитать оригинальный код** на GitHub
2. **Понять как работает** текущая логика
3. **Записать** что именно нужно изменить (точечно!)

### ПОСЛЕ завершения изменений:
1. **Сравнить** изменённый код с оригиналом на GitHub
2. **Убедиться** что:
   - Оригинальная логика НЕ сломана
   - Сделаны ТОЛЬКО нужные улучшения/доработки
   - Не удалены важные части кода случайно
3. **Если что-то удалено** — объяснить ПОЧЕМУ это безопасно

### ЗАПРЕЩЕНО:
- Переписывать функцию "с нуля" без сравнения с оригиналом
- Удалять код который "кажется лишним" без проверки
- Менять логику без понимания зачем она была

### Чек-лист после изменения критических функций:
```
[ ] Прочитал оригинал на GitHub перед изменением
[ ] Изменения точечные, не переписал всё
[ ] Сравнил результат с оригиналом
[ ] Оригинальная логика сохранена
[ ] Новый функционал добавлен корректно
```

### История инцидента (для памяти):
Claude при исправлении Bug 1 (слипание слов) случайно удалил всю логику
генерации перестановок слов в `extract_patterns_from_text()`.
Вместо 14 паттернов функция стала возвращать 1.
Причина: переписал функцию полностью вместо точечного фикса.

---

## 29. Разделение Handlers и Business Logic (ОБЯЗАТЕЛЬНО!)

**⚠️ КРИТИЧЕСКОЕ ПРАВИЛО — основа чистой архитектуры!**

### Принцип:
```
Handlers (тонкие)              Services (бизнес-логика)
─────────────────              ─────────────────────────
- Получить callback/message    - Валидация данных
- Извлечь параметры            - Работа с БД (CRUD)
- Вызвать service              - Расчёты, проверки
- Показать результат           - Применение действий (mute/ban)
- Обработать ошибки UI         - Бизнес-правила
```

### Handlers должны быть ТОНКИМИ:
- Максимум 15-25 строк кода
- Только: парсинг → вызов сервиса → ответ пользователю
- НЕ содержат бизнес-логику
- НЕ работают напрямую с БД (кроме простых случаев)

### Services содержат ВСЮ логику:
- Валидация входных данных
- Запросы к БД
- Бизнес-правила и расчёты
- Применение действий
- Возвращают готовый результат

### Пример НЕПРАВИЛЬНО:
```python
@router.callback_query(...)
async def set_action(callback: CallbackQuery, session: AsyncSession):
    # ❌ 50+ строк бизнес-логики прямо в хендлере
    chat_id = int(callback.data.split(":")[2])
    action = callback.data.split(":")[1]

    # Работа с БД прямо в хендлере
    result = await session.execute(
        select(ContentFilterSettings).where(...)
    )
    settings = result.scalar_one_or_none()
    if not settings:
        settings = ContentFilterSettings(chat_id=chat_id)
        session.add(settings)

    settings.action = action
    await session.commit()

    # Построение текста и клавиатуры
    text = f"Действие изменено на {action}\n..."
    keyboard = InlineKeyboardMarkup(...)

    await callback.message.edit_text(text, reply_markup=keyboard)
```

### Пример ПРАВИЛЬНО:
```python
# handlers/content_filter/action_handler.py (тонкий, ~15 строк)
@router.callback_query(F.data.startswith("cf:set_action:"))
async def set_action(callback: CallbackQuery, session: AsyncSession):
    # Парсим параметры
    chat_id, action = parse_action_callback(callback.data)

    # Вызываем сервис
    result = await content_filter_service.set_action(
        session=session,
        chat_id=chat_id,
        action=action
    )

    # Показываем результат
    await callback.message.edit_text(
        text=result.text,
        reply_markup=result.keyboard
    )
    await callback.answer(result.notification)


# services/content_filter/settings_service.py (бизнес-логика)
class ContentFilterSettingsService:
    async def set_action(
        self,
        session: AsyncSession,
        chat_id: int,
        action: str
    ) -> ActionResult:
        # Валидация
        if action not in ALLOWED_ACTIONS:
            raise ValueError(f"Invalid action: {action}")

        # Работа с БД
        settings = await self._get_or_create_settings(session, chat_id)
        settings.action = action
        await session.commit()

        # Формируем результат
        return ActionResult(
            text=self._format_settings_text(settings),
            keyboard=self._build_settings_keyboard(chat_id),
            notification="Действие изменено"
        )
```

### Структура файлов:
```
handlers/content_filter/
├── settings_handler.py      # Тонкие хендлеры (~200 строк)
├── words_handler.py         # Тонкие хендлеры (~150 строк)
└── ...

services/content_filter/
├── settings_service.py      # Бизнес-логика настроек
├── word_filter_service.py   # Бизнес-логика слов
├── filter_manager.py        # Координация фильтров
└── ...
```

### ЗАПРЕЩЕНО:
- Писать бизнес-логику в хендлерах
- Делать сложные запросы к БД в хендлерах
- Хендлеры больше 30 строк (признак проблемы)
- Дублировать логику между хендлерами

### Чек-лист при написании хендлера:
```
[ ] Хендлер <= 25 строк
[ ] Нет прямых запросов к БД (кроме простых)
[ ] Вся логика в сервисе
[ ] Хендлер только: парсинг → сервис → ответ
```

---

## 30. SRP — Single Responsibility Principle (ОБЯЗАТЕЛЬНО!)

**⚠️ КРИТИЧЕСКОЕ ПРАВИЛО — предотвращает проблемы с контекстом и поддержкой кода!**

### Проблема:
- Файл settings_handler.py вырос до 10,400 строк
- При работе с таким файлом контекст Claude быстро заполняется
- Claude перестаёт работать, сессия прерывается
- Поддерживать и понимать такой код крайне сложно

### ЛИМИТЫ размера файлов:

| Размер | Статус | Действие |
|--------|--------|----------|
| < 500 строк | ✅ OK | Норма |
| 500-800 строк | ⚠️ Warning | Подумать о разбиении |
| > 800 строк | 🔴 Critical | ОБЯЗАТЕЛЬНО предложить рефакторинг |

### ОБЯЗАННОСТИ Claude:

1. **При добавлении кода в файл > 500 строк:**
   - Предупредить: "Файл уже X строк, предлагаю вынести в отдельный модуль"
   - Спросить разрешение на создание нового файла

2. **При добавлении новой функциональности:**
   - СРАЗУ создавать в отдельном модуле
   - НЕ добавлять в существующий большой файл
   - Группировать по логике (word_filter/, scam/, flood/ и т.д.)

3. **При обнаружении большого файла:**
   - Сообщить о проблеме
   - Предложить план разбиения
   - Спросить: "Хочешь сделать рефакторинг сейчас или позже?"

### Структура модулей (пример):
```
handlers/content_filter/
├── __init__.py              # Только импорты и роутер
├── common.py                # Общие FSM states, helpers
├── main_menu.py             # Главное меню (~100 строк)
├── word_filter/             # Модуль фильтра слов
│   ├── settings.py          # Настройки (~200 строк)
│   ├── words.py             # Добавление слов (~300 строк)
│   └── categories.py        # Категории (~400 строк)
├── scam/                    # Модуль антискам
├── flood/                   # Модуль антифлуд
└── sections/                # Кастомные разделы
```

### ЗАПРЕЩЕНО:
- Добавлять новые функции в файл > 800 строк без обсуждения
- Игнорировать предупреждения о размере файла
- "Потом разобьём" — разбивать СРАЗУ или спрашивать

### Пример диалога:
```
Админ: "Добавь новую функцию X в settings_handler.py"

Claude: "⚠️ Файл settings_handler.py уже 10,400 строк — это критически много.
        Предлагаю:
        1. Вынести функцию X в отдельный файл new_module.py
        2. Или сначала сделать рефакторинг большого файла

        Как поступим?"
```

### Почему это важно:
- Большие файлы = быстрое заполнение контекста Claude
- Сессия прерывается на середине работы
- Код сложно понимать и поддерживать
- Баги сложнее находить и исправлять
- Рефакторинг откладывается бесконечно

**ВЫВОД:** Лучше потратить 10 минут на создание нового модуля сейчас,
чем 2 часа на рефакторинг файла в 10,000 строк потом.

---

## 31. ⛔ НЕ МЕНЯТЬ ПОВЕДЕНИЕ БЕЗ ЯВНОГО СОГЛАСИЯ (MUST!!!)

**⚠️ КРИТИЧЕСКОЕ ПРАВИЛО — нарушение привело к потере пользователей на месяц!**

### История инцидента (ноябрь 2025):
```
Задача: исправить что-то в модуле капчи
Что сделал Claude: "между строк" добавил auto-decline join requests при провале
Результат: бот МЕСЯЦ отклонял заявки пользователей которые не прошли капчу
Последствия: пользователи терялись, статистика упала
Когда заметили: только через МЕСЯЦ, когда админ увидел падение
```

**ЭТО НЕДОПУСТИМО! НИКОГДА ТАК НЕ ДЕЛАТЬ!**

### ❌ КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО:
1. **Добавлять функционал который НЕ просили** — даже если кажется полезным
2. **Менять логику "потому что так лучше"** — это не твоё решение
3. **Додумывать за пользователя** — спроси, не угадывай
4. **"Улучшать" код попутно** — делай только то что просят
5. **Читать между строк** — понимай буквально

### ✅ ОБЯЗАТЕЛЬНО:
1. **Делать РОВНО то что просят** — не больше, не меньше
2. **Спрашивать перед изменением поведения** — "Сейчас сделаю X, это ок?"
3. **Просят текст — меняй ТОЛЬКО текст** — не логику
4. **Просят баг — фикси ТОЛЬКО баг** — не рефактори вокруг
5. **Сомневаешься — СПРОСИ** — лучше лишний вопрос чем сломанный код

### Примеры:

**НЕПРАВИЛЬНО:**
```
Админ: "Измени текст сообщения о таймауте"
Claude: *меняет текст И добавляет удаление сообщения*
        *меняет текст И рефакторит функцию*
        *меняет текст И "оптимизирует" логику*
```

**ПРАВИЛЬНО:**
```
Админ: "Измени текст сообщения о таймауте"
Claude: *меняет ТОЛЬКО текст*
Claude: "Текст изменён. Заметил что можно ещё X улучшить — сделать?"
```

### Чек-лист перед каждым изменением:
```
[ ] Это именно то что просили?
[ ] Я не добавляю ничего лишнего?
[ ] Я не меняю поведение "заодно"?
[ ] Если меняю логику — я спросил разрешение?
[ ] Я не додумываю за админа?
```

### Если очень хочется "улучшить":
1. Сделай то что просили
2. ОТДЕЛЬНО скажи: "Заметил что можно улучшить X. Хочешь?"
3. Дождись ответа
4. Только потом делай (если разрешили)

**ВЫВОД:** Лучше спросить 10 раз, чем сломать бота на месяц!

---

## 32. CPU-heavy операции — НЕ блокировать Event Loop (ОБЯЗАТЕЛЬНО!)

**⚠️ КРИТИЧЕСКОЕ ПРАВИЛО для asyncio приложений!**

### Проблема:
Python asyncio работает в **одном потоке**. Если запустить тяжёлую CPU операцию — весь бот "замирает" и не обрабатывает другие сообщения.

### Примеры CPU-heavy операций:
- `SequenceMatcher`, `difflib` — сравнение строк
- `re.search()` в циклах на длинных текстах
- Fuzzy matching (нечёткий поиск)
- Обработка изображений
- Сложные вычисления в циклах
- JSON/XML парсинг больших файлов

### ОБЯЗАТЕЛЬНО:

1. **Использовать быстрые библиотеки на C/Cython:**
   ```python
   # ❌ ПЛОХО: медленный чистый Python
   from difflib import SequenceMatcher

   # ✅ ХОРОШО: быстрая C-библиотека
   from rapidfuzz import fuzz
   ```

2. **Выносить CPU-heavy в ThreadPoolExecutor:**
   ```python
   import asyncio
   from concurrent.futures import ThreadPoolExecutor

   # Создаём пул потоков (обычно при старте приложения)
   executor = ThreadPoolExecutor(max_workers=4)

   # ❌ ПЛОХО: блокирует event loop
   result = heavy_cpu_function(data)

   # ✅ ХОРОШО: выполняется в отдельном потоке
   loop = asyncio.get_event_loop()
   result = await loop.run_in_executor(executor, heavy_cpu_function, data)
   ```

3. **Кэшировать данные в памяти:**
   ```python
   # ❌ ПЛОХО: запрос к БД на каждое сообщение
   patterns = await get_patterns_from_db(chat_id)

   # ✅ ХОРОШО: кэш в памяти с TTL
   patterns = await get_patterns_cached(chat_id)  # Redis или lru_cache
   ```

### Чек-лист при написании нового кода:
```
[ ] Есть ли циклы с большим количеством итераций?
[ ] Есть ли сравнение строк (fuzzy, diff)?
[ ] Есть ли обработка больших данных?
[ ] Если ДА → использовать ThreadPoolExecutor
[ ] Данные из БД кэшируются?
```

### Как проверить что код блокирует:
```python
import time

start = time.time()
# твой код
elapsed = time.time() - start

if elapsed > 0.1:  # больше 100ms
    print(f"⚠️ БЛОКИРОВКА: {elapsed:.2f} сек")
```

---

## 33. Git Commit — ОБЯЗАТЕЛЬНАЯ ПРОВЕРКА НОВЫХ ФАЙЛОВ

### ⚠️ КРИТИЧЕСКАЯ ОШИБКА (02.01.2026)

**Что произошло:** Бот на проде упал потому что новый файл `antispam_journal_actions.py`
не был добавлен в коммит, хотя он импортировался в `__init__.py`.

**Причина:** При `git add` были добавлены только modified файлы, но не новые (untracked).

### ❌ ЗАПРЕЩЕНО:

```bash
# НЕ делать так - добавляем только конкретные файлы без проверки
git add file1.py file2.py
git commit -m "..."
git push
```

### ✅ ОБЯЗАТЕЛЬНЫЙ ПОРЯДОК:

```bash
# 1. Смотрим ВСЕ изменения включая untracked
git status

# 2. Проверяем: есть ли НОВЫЕ файлы которые импортируются?
#    Смотрим untracked files!

# 3. Добавляем ВСЕ нужные файлы (включая новые)
git add file1.py file2.py new_file.py

# 4. ПРОВЕРЯЕМ после git add
git status
# Убедиться что все импортируемые файлы в "Changes to be committed"

# 5. Только потом коммит
git commit -m "..."
git push
```

### Чек-лист перед коммитом:

```
[ ] Запустил git status
[ ] Проверил секцию "Untracked files"
[ ] Новые файлы которые импортируются — добавлены в git add
[ ] После git add ещё раз проверил git status
[ ] Все нужные файлы в "Changes to be committed"
```

### Как найти пропущенные импорты:

```bash
# Смотрим какие файлы импортируются в изменённых __init__.py
git diff --cached | grep "^+.*import"
```

**ПОМНИ:** Один пропущенный файл = бот на проде падает!

---

## 34. ExportableMixin — автоматический экспорт настроек (ОБЯЗАТЕЛЬНО!)

**⚠️ КРИТИЧЕСКОЕ ПРАВИЛО — чтобы настройки групп копировались корректно!**

### Что это:
Миксин `ExportableMixin` автоматически регистрирует модели для экспорта/импорта настроек группы.
Когда админ скачивает настройки и загружает на другую группу — экспортируются ВСЕ модели с этим миксином.

### КОГДА добавлять миксин:
Добавлять `ExportableMixin` к модели если она:
- Хранит **настройки группы** (ChatSettings, ContentFilterSettings, etc.)
- Хранит **данные для группы** (FilterWord, ScamPattern, AntiSpamRule, etc.)
- Должна **копироваться** при переносе настроек на другую группу

### КОГДА НЕ добавлять миксин:
- **Логи/история** (FilterViolation, ProfileChangeLog) — не нужно копировать
- **Статистика** (UserStats) — уникальна для группы
- **Глобальные данные** (SpammerRecord) — не привязаны к группе
- **Временные данные** (кэши, сессии)

### КАК добавить миксин к новой модели:

**Шаг 1:** Добавить миксин и настройки в модель:
```python
from bot.database.exportable_mixin import ExportableMixin

class MyNewModel(Base, ExportableMixin):
    __tablename__ = 'my_table'

    # ─── Настройки экспорта ───
    __export_key__ = 'my_models'           # Уникальный ключ в JSON
    __export_order__ = 450                 # Порядок импорта (меньше = раньше)
    __export_is_settings__ = False         # True если одна запись на группу
    # __export_exclude__ = ('field1',)     # Поля которые НЕ экспортировать
    # __export_chat_id_column__ = 'chat_id'  # Колонка с ID группы (по умолчанию)

    # Остальные поля модели...
    chat_id = Column(BigInteger, ...)
```

**Шаг 2:** Добавить импорт в `bot/services/settings_export/export_service.py`:
```python
# В секции импортов моделей
from bot.database.models_xxx import MyNewModel  # noqa: F401
```

### Параметры миксина:

| Параметр | Обязательный | Описание |
|----------|--------------|----------|
| `__export_key__` | ✅ ДА | Уникальный ключ в JSON (например: 'filter_words') |
| `__export_order__` | ❌ нет | Порядок импорта: 0-99 настройки, 100-199 модули, 200+ данные |
| `__export_is_settings__` | ❌ нет | True = одна запись на группу (настройки) |
| `__export_exclude__` | ❌ нет | Tuple полей для исключения: `('id', 'created_at')` |
| `__export_chat_id_column__` | ❌ нет | Имя колонки chat_id (по умолчанию 'chat_id') |
| `__export_parent_key__` | ❌ нет | Для дочерних моделей: ключ родителя |
| `__export_parent_column__` | ❌ нет | Для дочерних моделей: колонка FK |

### Рекомендуемые значения `__export_order__`:

| Диапазон | Тип моделей | Примеры |
|----------|-------------|---------|
| 0-99 | Основные настройки | ChatSettings (10), CaptchaSettings (20) |
| 100-199 | Настройки модулей | ContentFilterSettings (100), ProfileMonitorSettings (110) |
| 200-299 | Родительские данные | CustomSpamSection (200) |
| 300-399 | Дочерние данные | CustomSectionPattern (300), CustomSectionThreshold (301) |
| 400-499 | Обычные данные | FilterWord (410), AntiSpamRule (430) |

### Пример для дочерней модели (parent-child):
```python
class CustomSectionPattern(Base, ExportableMixin):
    __tablename__ = 'custom_section_patterns'

    # ─── Настройки экспорта ───
    __export_key__ = 'custom_section_patterns'
    __export_order__ = 300  # После родителя (200)
    __export_parent_key__ = 'custom_spam_sections'  # Ключ родителя
    __export_parent_column__ = 'section_id'         # FK колонка

    section_id = Column(Integer, ForeignKey("custom_spam_sections.id"))
```

### Чек-лист при создании новой модели с chat_id:

```
[ ] Модель хранит данные/настройки группы?
[ ] Эти данные должны копироваться при экспорте?
    ↓ Если ДА на оба вопроса:
[ ] Добавил ExportableMixin к классу
[ ] Определил __export_key__ (уникальный!)
[ ] Определил __export_order__ (правильный диапазон)
[ ] Если settings — добавил __export_is_settings__ = True
[ ] Добавил импорт в export_service.py
[ ] Проверил что импорты работают
```

### ЗАПРЕЩЕНО:
- Создавать модель с chat_id без обсуждения нужен ли экспорт
- Забывать добавлять импорт в export_service.py
- Использовать одинаковые __export_key__ для разных моделей

---

*Последнее обновление: 2026-01-02 (добавлен раздел 34 — ExportableMixin для экспорта настроек)*