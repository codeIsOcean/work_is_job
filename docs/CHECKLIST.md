
те# Чек-лист проверки завершённых задач

> **Использование:** После завершения любой задачи Claude должен пройти по этому чек-листу.
> Это предотвращает типичные ошибки, которые уже случались в проекте.

---

## 1. UX/UI проверки

### 1.1 Dead-End UX
- [ ] Все callback_data в кнопках имеют соответствующие обработчики
- [ ] Нет кнопок которые ведут в никуда
- [ ] Из любого экрана можно вернуться назад
- [ ] Информационные кнопки имеют обработчик `noop` (просто `callback.answer()`)

### 1.2 Кнопки "Назад"
- [ ] Используется текст "🔙 Назад" или "🔙 Назад к..." (НЕ "Отмена", НЕ "❌")
- [ ] Кнопка "Назад" есть на каждом вложенном экране
- [ ] Кнопка "Назад" возвращает к правильному родительскому экрану

### 1.3 FSM и состояния
- [ ] При нажатии "Назад" вызывается `await state.clear()`
- [ ] FSM состояние очищается при успешном завершении ввода
- [ ] FSM состояние очищается при ошибках
- [ ] Висящие сообщения в диалоге удаляются (если применимо)

---

## 2. Код и синтаксис

### 2.1 Синтаксис Python
- [ ] Все новые/изменённые файлы проверены: `python -m py_compile файл.py`
- [ ] Нет синтаксических ошибок
- [ ] Нет опечаток в именах переменных/функций

### 2.2 Импорты
- [ ] Все необходимые модули импортированы
- [ ] Нет circular imports (проверить запуском бота)
- [ ] Импорты отсортированы (стандартные → сторонние → локальные)

### 2.3 Роутеры и хендлеры
- [ ] Новый роутер зарегистрирован в `handlers/__init__.py`
- [ ] Роутер добавлен в обе функции: `handlers_router` и `create_fresh_handlers_router()`
- [ ] Callback handlers используют правильные фильтры (`F.data == "..."` или `F.data.startswith("...")`)

---

## 3. База данных

### 3.1 Миграции Alembic
- [ ] Миграция создана если есть изменения в моделях
- [ ] `down_revision` указывает на правильную предыдущую миграцию
- [ ] ENUM типы создаются идемпотентно (DO $$ BEGIN ... EXCEPTION ... END $$)
- [ ] Миграция проверена локально: `alembic upgrade head`
- [ ] downgrade() корректно откатывает изменения

### 3.2 Модели SQLAlchemy
- [ ] Новые модели добавлены в соответствующий файл models_*.py
- [ ] Колонки имеют правильные типы и constraints
- [ ] server_default указан для колонок с дефолтными значениями
- [ ] Индексы созданы для часто используемых в WHERE колонок

---

## 4. Экспорт настроек

### 4.1 ExportableMixin (для настроек групп)
- [ ] Если модель привязана к `chat_id` — добавить `ExportableMixin`
- [ ] Модель импортирована в `services/settings_export/export_service.py`
- [ ] Указаны `__export_key__`, `__export_chat_id_column__` и другие атрибуты

### 4.2 Глобальные настройки
- [ ] Глобальные настройки (singleton без chat_id) НЕ добавляются в экспорт групп
- [ ] Если нужен экспорт глобальных настроек — создать отдельный механизм

---

## 5. Документация

### 5.1 Обновление docs/
- [ ] `MODULES.md` — добавлен новый модуль
- [ ] `HANDLERS.md` — добавлены новые хендлеры (если есть)
- [ ] `DATABASE.md` — добавлены новые таблицы (если есть)

### 5.2 Комментарии в коде
- [ ] Каждая функция имеет docstring
- [ ] Сложная логика прокомментирована
- [ ] Комментарии на русском (согласно DEVELOPER_RULES.md)

---

## 6. Безопасность и Edge Cases

### 6.1 Проверка прав
- [ ] Хендлеры проверяют права пользователя (ADMIN_IDS, права в группе)
- [ ] Нет возможности выполнить действие без прав

### 6.2 Edge Cases
- [ ] Что будет если пользователь заблокировал бота?
- [ ] Что будет если Telegram API вернул ошибку?
- [ ] Что будет если данные в БД отсутствуют?
- [ ] **НИКОГДА** не делать auto-approve/auto-action как fallback при ошибке!

### 6.3 Обработка ошибок
- [ ] try/except блоки не глотают ошибки молча
- [ ] Ошибки логируются с достаточным контекстом
- [ ] Пользователю показывается понятное сообщение об ошибке

---

## 7. Telegram лимиты

### 7.1 callback_data
- [ ] Длина callback_data ≤ 64 байта (лимит Telegram!)
- [ ] Используются короткие префиксы (cg:, cf:, as:)
- [ ] ID в callback_data не слишком длинные

### 7.2 Тексты сообщений
- [ ] Текст сообщения ≤ 4096 символов
- [ ] Caption для медиа ≤ 1024 символов
- [ ] Кнопки: текст ≤ 64 символов

---

## 8. Логирование и отладка

### 8.1 Логи в коде (logger)
- [ ] Добавлены debug/info логи для отслеживания флоу
- [ ] Ошибки логируются с контекстом (user_id, chat_id)
- [ ] Используется правильный уровень (debug/info/warning/error)
- [ ] При важных действиях есть logger.info (мут, бан, детекция)
- [ ] При ошибках есть logger.error с описанием что пошло не так

### 8.2 Журнал группы (КРИТИЧНО!)
- [ ] **Если модуль выполняет действия (мут/бан/детекция) — ОБЯЗАТЕЛЬНО отправлять в журнал!**
- [ ] Функция отправки в журнал написана И вызывается (проверить!)
- [ ] Журнал привязывается через `/linkjournal` в группе
- [ ] Сообщение в журнале содержит: кто, что, когда, почему
- [ ] Есть кнопки для действий (размут, бан, OK)
- [ ] Используется `get_journal_channel_for_group()` для получения ID журнала

### 8.3 Обработка Telegram ошибок
- [ ] TelegramAPIError обрабатывается
- [ ] "message is not modified" игнорируется корректно
- [ ] "bot was blocked by the user" не ломает флоу
- [ ] Ошибка отправки в журнал не ломает основной флоу

---

## 9. Redis и состояния

### 9.1 Redis ключи
- [ ] Ключи имеют уникальные префиксы (не конфликтуют с существующими)
- [ ] TTL установлен для временных данных
- [ ] Формат: `module:entity:id` (например `captcha:user:123`)

### 9.2 FSM состояния
- [ ] Названия состояний уникальны в рамках модуля
- [ ] StatesGroup имеет понятное имя

---

## 10. Финальные проверки

### 10.1 Перед коммитом
- [ ] Все файлы сохранены
- [ ] `git status` — только нужные файлы
- [ ] Нет файлов с секретами (.env, credentials)
- [ ] Нет временных/тестовых файлов
- [ ] **НИКОГДА не писать "Claude" в коммитах!**
- [ ] Нет Co-Authored-By с упоминанием AI
- [ ] Нет комментариев типа "// Added by Claude"

### 10.2 Соответствие задаче
- [ ] Сделано РОВНО то, что просили (не больше, не меньше)
- [ ] Не добавлен лишний функционал "для улучшения"
- [ ] Не изменено поведение которое не просили менять

### 10.3 Тестирование
- [ ] Код запускается без ошибок импорта
- [ ] Базовый флоу работает (если можно проверить)
- [ ] Нет circular imports

---

## Быстрый чек-лист (копировать в ответ)

```
Проверка завершённой задачи:

UX/UI:
- [ ] Dead-End UX: все кнопки имеют обработчики
- [ ] Кнопки "Назад" (НЕ "Отмена"!), FSM очищается
- [ ] callback_data ≤ 64 байта

Код:
- [ ] Синтаксис: python -m py_compile ✓
- [ ] Импорты работают, нет circular imports
- [ ] Роутер зарегистрирован в handlers/__init__.py

БД:
- [ ] Миграция: down_revision верный
- [ ] ENUM создаётся идемпотентно

Логи и журнал:
- [ ] logger.info/error добавлены для важных действий
- [ ] Журнал: функция отправки НАПИСАНА и ВЫЗЫВАЕТСЯ!
- [ ] Журнал привязывается через /linkjournal

Интеграция:
- [ ] Экспорт: модель добавлена (если chat_id)
- [ ] Документация: MODULES.md обновлён

Безопасность:
- [ ] Edge cases проверены (бот заблокирован, API ошибки)
- [ ] Права проверяются

Коммит:
- [ ] НЕТ слова "Claude" в коммите!
- [ ] НЕТ Co-Authored-By AI
```

---

*Создано: 2026-01-17*
*Последнее обновление: 2026-01-17*