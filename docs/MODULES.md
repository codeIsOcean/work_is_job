# Modules / Модули

## Overview

Описание ключевых модулей бота и их взаимодействия.

---

## 1. Captcha / Капча (РЕДИЗАЙН 2025-12)

### Назначение
Защита групп от спам-ботов путём проверки новых участников.

### Три режима капчи

| Режим | Триггер | Куда отправляется | Требования |
|-------|---------|-------------------|------------|
| **Join Request Captcha** | `chat_join_request` | В ЛС пользователю | Join Requests в группе (закрытая группа) |
| **Join Captcha** | `new_chat_members` (самовход) | В группу → deep link в ЛС | Открытая группа |
| **Invite Captcha** | `new_chat_members` (инвайт) | В группу → deep link в ЛС | Любая группа, всегда работает |

### Логика работы
- **Закрытая группа** (join request enabled) → капча сразу в ЛС
- **Открытая группа** (join request disabled) → капча в группе с кнопкой "Решить капчу" → deep link в ЛС
- **Invite Captcha** работает ВСЕГДА, независимо от настроек группы

### Файлы

**Сервисы (services/captcha/):**
| Файл | Назначение |
|------|------------|
| `services/captcha/__init__.py` | Экспорт модуля |
| `services/captcha/settings_service.py` | CRUD настроек, CaptchaMode enum, CaptchaSettings dataclass |
| `services/captcha/flow_service.py` | Бизнес-логика отправки/проверки |
| `services/captcha/verification_service.py` | Проверка ответов, ownership check |
| `services/captcha/cleanup_service.py` | Очистка капч, лимиты, Redis |
| `services/captcha/dm_flow_service.py` | Логика ЛС капчи, deep links, Redis операции |
| `services/captcha/reminder_service.py` | Напоминания и таймауты (asyncio.create_task) |

**Хендлеры (handlers/captcha/):**
| Файл | Назначение |
|------|------------|
| `handlers/captcha/__init__.py` | Экспорт captcha_router |
| `handlers/captcha/captcha_coordinator.py` | **ЕДИНАЯ ТОЧКА ВХОДА** (chat_join_request, new_chat_members) |
| `handlers/captcha/captcha_callbacks.py` | Callback обработчики (verify, настройки, диалоги) |
| `handlers/captcha/captcha_fsm_handler.py` | FSM для deep link и ручного ввода ответа |
| `handlers/captcha/captcha_keyboards.py` | Клавиатуры (2 кнопки в ряд) |
| `handlers/captcha/captcha_messages.py` | Тексты сообщений капчи |

### Pipeline (новый)

```
chat_join_request / new_chat_members
    │
    ├── captcha_coordinator.py (единая точка входа)
    │
    ├── check_and_restore_restriction() → проверка активного мута
    │   └── Если есть мут → восстанавливаем, отклоняем
    │
    ├── determine_captcha_mode() → выбор режима
    │   ├── join_request → VISUAL_DM (если включён)
    │   ├── self_join → JOIN_GROUP (если включён)
    │   └── invite → INVITE_GROUP (если включён)
    │
    └── send_captcha()
        ├── cleanup_user_captcha() → очистка старой
        ├── enforce_captcha_limit() → проверка лимита
        ├── generate_visual_captcha() → создание
        └── отправка в ЛС или группу
```

### Настройки (ChatSettings)

**Основные настройки:**
| Настройка | Описание | Default |
|-----------|----------|---------|
| `visual_captcha_enabled` | Join Request Captcha (ЛС) вкл/выкл | False |
| `visual_captcha_timeout_seconds` | Таймаут Join Request Captcha | 120 |
| `join_captcha_timeout_seconds` | Таймаут Join Captcha | 120 |
| `invite_captcha_timeout_seconds` | Таймаут Invite Captcha | 120 |
| `captcha_max_pending` | Макс. одновременных капч | None |
| `captcha_overflow_action` | Действие при переполнении | None |

**Настройки диалогов (НОВЫЕ):**
| Настройка | Описание | Default |
|-----------|----------|---------|
| `captcha_manual_input_enabled` | Ручной ввод ответа | True |
| `captcha_button_count` | Количество кнопок (4/6/9) | 6 |
| `captcha_max_attempts` | Макс. попыток | 3 |
| `captcha_reminder_seconds` | Время до напоминания (0=выкл) | 60 |
| `captcha_dialog_cleanup_seconds` | Время до очистки диалога | 120 |

**Legacy настройки (сохранены для совместимости):**
| Настройка | Описание |
|-----------|----------|
| `captcha_join_enabled` | Join Captcha вкл/выкл |
| `captcha_invite_enabled` | Invite Captcha вкл/выкл |
| `captcha_timeout_seconds` | Legacy общий таймаут |
| `captcha_message_ttl_seconds` | TTL сообщения |
| `captcha_flood_threshold` | Порог антифлуда |
| `captcha_flood_window_seconds` | Окно антифлуда |
| `captcha_flood_action` | Действие антифлуда |

### Структура меню настроек

```
⚙️ Настройки капчи
├── ✅/❌ Join Request Captcha (ЛС)
├── ✅/❌ Join Captcha (группа, самовход)
├── ✅/❌ Invite Captcha (группа, инвайт)
├── ─────────────────
├── 📊 Макс. капч: X
├── ⚡ При переполнении: действие
├── ─────────────────
├── 💬 Настройки диалогов
│   ├── ✅/❌ Ручной ввод
│   ├── 🔢 Кнопок: 6
│   ├── 🔄 Попыток: 3
│   ├── 🔔 Напоминание: 60 сек
│   └── 🧹 Чистка: 120 сек
└── ◀️ Назад
```

### Redis Keys (новые)
```python
# Данные активной капчи
f"captcha:data:{user_id}:{chat_id}"

# ID сообщения с капчей
f"captcha:msg:{user_id}:{chat_id}"

# Счётчик попыток
f"captcha:attempts:{user_id}:{chat_id}"

# Владелец капчи (для проверки кнопок)
f"captcha:owner:{chat_id}:{message_id}"
```

### Защита от багов

1. **Персистентность мута** - проверка `UserRestriction` при каждом входе
2. **Проверка владельца** - `owner_id` в callback_data
3. **Лимит капч** - `captcha_max_pending` + `overflow_action`
4. **Очистка старых** - `cleanup_user_captcha()` перед новой

---

## 2. Antispam / Антиспам

### Назначение
Фильтрация сообщений по правилам: ссылки, пересылки, цитаты.

### Файлы
| Файл | Назначение |
|------|------------|
| `handlers/antispam_handlers/antispam_filter_handler.py` | Фильтр сообщений |
| `handlers/antispam_handlers/antispam_settings_handler.py` | UI настроек |
| `services/antispam/antispam_service.py` | Бизнес-логика |
| `database/models_antispam.py` | Модели данных |
| `keyboards/antispam_keyboards.py` | Клавиатуры |

### Типы правил (RuleType)

| Тип | Описание |
|-----|----------|
| `TELEGRAM_LINK` | Ссылки t.me, telegram.me, tg:// |
| `ANY_LINK` | Любые HTTP/HTTPS ссылки |
| `FORWARD_CHANNEL` | Пересылки из каналов |
| `FORWARD_GROUP` | Пересылки из групп |
| `FORWARD_USER` | Пересылки от пользователей |
| `FORWARD_BOT` | Пересылки от ботов |
| `QUOTE_CHANNEL` | Цитаты из каналов |
| `QUOTE_GROUP` | Цитаты из групп |
| `QUOTE_USER` | Цитаты от пользователей |
| `QUOTE_BOT` | Цитаты от ботов |

### Действия (ActionType)

| Действие | Описание |
|----------|----------|
| `OFF` | Правило выключено |
| `DELETE` | Только удаление сообщения |
| `WARN` | Предупреждение |
| `KICK` | Исключение из группы |
| `RESTRICT` | Мут на время |
| `BAN` | Бан навсегда |

### Pipeline фильтра

```
Сообщение в группе
    │
    ├── Проверка: group/supergroup?
    ├── Загрузка правил из БД
    │
    ├── check_links()
    │   ├── Извлечение URL из текста
    │   ├── Проверка TELEGRAM_LINK
    │   └── Проверка ANY_LINK
    │
    ├── check_forwards()
    │   ├── forward_from_chat? → FORWARD_CHANNEL/GROUP
    │   ├── forward_from.is_bot? → FORWARD_BOT
    │   └── forward_from? → FORWARD_USER
    │
    ├── check_quotes()
    │   └── Аналогично пересылкам
    │
    ├── check_whitelist() → исключения
    │
    └── apply_action()
        ├── DELETE → message.delete()
        ├── WARN → отправка предупреждения
        ├── KICK → bot.ban + bot.unban
        ├── RESTRICT → bot.restrict_chat_member
        └── BAN → bot.ban_chat_member
```

### Белый список (Whitelist)

| Scope | Примеры паттернов |
|-------|-------------------|
| `TELEGRAM_LINK` | `t.me/mygroup`, `@channel` |
| `ANY_LINK` | `youtube.com`, `github.com` |
| `FORWARD` | `-1001234567890` (chat ID) |
| `QUOTE` | `-1001234567890` |

---

## 3. Content Filter / Фильтр контента

### Назначение
Фильтрация сообщений по запрещённым словам с обходом обфускации (l33tspeak).

### Файлы
| Файл | Назначение |
|------|------------|
| `handlers/content_filter/filter_handler.py` | Фильтр сообщений в группах |
| `handlers/content_filter/settings_handler.py` | UI настроек (callback query) |
| `services/content_filter/text_normalizer.py` | Нормализация текста (l33tspeak) |
| `services/content_filter/word_filter.py` | Проверка на запрещённые слова |
| `services/content_filter/scam_detector.py` | Эвристический детектор скама |
| `services/content_filter/flood_detector.py` | Детектор повторяющихся сообщений |
| `services/content_filter/referral_detector.py` | Детектор реферального спама |
| `services/content_filter/filter_manager.py` | Координация всех фильтров |
| `database/models_content_filter.py` | Модели данных |
| `keyboards/content_filter_keyboards.py` | Клавиатуры |

### Подмодули

| Подмодуль | Описание | Статус |
|-----------|----------|--------|
| Word Filter | Фильтрация по словам/фразам | ✅ Реализован |
| Scam Detector | Эвристическое определение скама (4 метода детекции + категории) | ✅ Реализован |
| Flood Detector | Определение флуда (Redis) — одинаковые, любые, медиа | ✅ Реализован |
| Message Cleanup | Удаление команд и системных сообщений | ✅ Реализован |
| Referral Detector | Детектор реферального спама (@username) | ✅ Реализован |

### Text Normalizer - Нормализация текста

**Проблема:** Спамеры обходят фильтры через:
- L33tspeak: `k0ka` вместо `кока`
- Разделители: `к-о-к-а` вместо `кока`
- Латиницу: `шиshkи` (латинские s, h, k вместо кириллицы)
- Невидимые символы: `ко\u200bка`
- **Combining Marks:** `ш̶u̶ш̶к̶u̶` (зачёркивание), `M͟͞n͟͞o͟͞g͟͞o͟͞e͟͞` (подчёркивание)
- **Unicode варианты:** `ⓚⓞⓚⓐ` (circled), `ｋｏｋａ` (fullwidth), `𝐤𝐨𝐤𝐚` (math bold)
- **Small Caps:** `ᴋᴏᴋᴀ`
- **Block Elements:** `░C░o░c░o`, `▒`, `▓`

**Решение:** TextNormalizer приводит текст к единому виду:

```python
normalizer = TextNormalizer()
normalizer.normalize("k0-k-@")      # → "кока"
normalizer.normalize("ш1шk1")       # → "шишки" (1→и, k→к)
normalizer.normalize("н@рк0т1к")    # → "наркотик"
normalizer.normalize("ш̶u̶ш̶к̶u̶")      # → "шушку" (зачёркивание удаляется, u→у)
normalizer.normalize("ⓚⓞⓚⓐ")        # → "кока" (circled → обычные)
normalizer.normalize("ᴋᴏᴋᴀ")        # → "кока" (small caps)
```

**Этапы нормализации:**
1. **NFKD** — раскладка Unicode (fullwidth, circled, math → обычные)
2. **Lowercase** — приведение к нижнему регистру
3. **Combining Marks** — удаление зачёркиваний, подчёркиваний (категории Mn, Mc)
4. **Invisible chars** — удаление zero-width символов
5. **Separators** — удаление разделителей между буквами (-, ░, •, и т.д.)
6. **Char map** — замена похожих символов (l33tspeak, small caps)

**Таблица замен:**
| Символ | Замена | Символ | Замена |
|--------|--------|--------|--------|
| `0` | `о` | `1` | `и` |
| `3` | `з` | `@` | `а` |
| `4` | `ч` | `$` | `с` |
| `k` | `к` | `u` | `у` |
| `ᴋ` | `к` | `ᴏ` | `о` |
| `ᴀ` | `а` | `ᴍ` | `м` |

### Word Filter - Фильтр слов

**Типы совпадений:**
| Тип | Описание | Пример |
|-----|----------|--------|
| `word` | Точное совпадение слова | "наркотик" ≠ "наркотики" |
| `phrase` | Подстрока | "кок" найдётся в "кокаин" |
| `regex` | Регулярное выражение | `нарк.?тик` |

### Действия (action)
| Действие | Описание |
|----------|----------|
| `delete` | Только удаление сообщения |
| `warn` | Удаление + предупреждение |
| `mute` | Удаление + мут на время |
| `kick` | Удаление + исключение |
| `ban` | Удаление + бан |

### Scam Detector - 4 метода детекции

**Проблема:** Спамеры перефразируют скам-сообщения, обходя точное совпадение паттернов.

**Решение:** 4 метода детекции работают параллельно:

| Метод | Описание | Применение |
|-------|----------|------------|
| **Keywords** | Точное вхождение ключевых слов | Базовая детекция: "наркотики", "drugs" |
| **Fuzzy** | Нечёткое сравнение (SequenceMatcher, порог 0.8) | Опечатки: "narkotik" → "наркотик" |
| **N-grams** | Биграммы/триграммы с порогом совпадения 0.5 | Перефразировки: "продаю товар" |
| **Categories** | Категории сигналов из БД с весами | Настраиваемые паттерны |

**Функции в `scam_detector.py`:**
```python
def fuzzy_match(text: str, pattern: str, threshold: float = 0.8) -> bool:
    """Нечёткое сравнение через SequenceMatcher."""

def extract_ngrams(text: str, n: int = 2) -> Set[str]:
    """Извлечение биграмм/триграмм из текста."""

def ngram_match(text_ngrams: Set[str], pattern_ngrams: Set[str], min_overlap: float = 0.5) -> bool:
    """Сравнение n-грамм с порогом совпадения."""
```

### Категории сигналов (Signal Categories)

**Назначение:** Настраиваемые категории для детекции скама с весами.

**Модель БД:**
```python
class ScamSignalCategory:
    id: Integer (PK)
    chat_id: BigInteger (FK → groups)
    category_name: String  # Название категории
    keywords: Text  # Ключевые слова через запятую
    weight: Integer  # Вес при подсчёте score (default: 25)
    enabled: Boolean  # Включена/выключена
    created_at: DateTime
```

**UI управления:**
```
Настройки антискама → 📂 Категории сигналов
├── ➕ Добавить категорию
│   ├── Ввод названия (FSM)
│   └── Ввод ключевых слов через запятую (FSM)
├── [Категория 1] ✅/❌
│   ├── Редактировать
│   ├── Вкл/Выкл
│   └── Удалить
└── ◀️ Назад
```

**Пример использования:**
1. Создать категорию "Наркотики"
2. Добавить ключевые слова: `drugs, наркота, weed, hashish, cocaine, crystal`
3. Установить вес: 40 баллов
4. При обнаружении любого ключевого слова — +40 к score

### Pipeline фильтра

```
Сообщение в группе
    │
    ├── Проверка: group/supergroup?
    ├── Проверка: автор - админ? → пропуск
    │
    ├── FilterManager.check_message()
    │   ├── Загрузка настроек группы
    │   │
    │   ├── 1. FloodDetector.check() (самый быстрый)
    │   │   ├── Вычисление хэша (MD5)
    │   │   ├── Проверка счётчика в Redis
    │   │   └── Сравнение с flood_max_repeats
    │   │
    │   ├── 2. Нормализация текста (TextNormalizer)
    │   │
    │   ├── 3. WordFilter.check()
    │   │   ├── Загрузка слов из БД
    │   │   ├── Загрузка whitelist
    │   │   └── Поиск совпадений
    │   │
    │   ├── 4. ScamDetector.check()
    │   │   ├── Анализ 15 сигналов
    │   │   ├── Подсчёт score
    │   │   └── Сравнение с scam_sensitivity
    │   │
    │   └── 5. ReferralDetector.check()
    │       ├── Извлечение @username
    │       ├── Проверка счётчиков в Redis
    │       └── Проверка unique_users
    │
    └── apply_action()
        ├── delete → message.delete()
        ├── warn → предупреждение
        ├── mute → restrict_chat_member
        ├── kick → ban + unban
        └── ban → ban_chat_member
```

### Настройки (ContentFilterSettings)
| Настройка | Описание | Default |
|-----------|----------|---------|
| `enabled` | Фильтр включён | False |
| `word_filter_enabled` | Фильтр слов | True |
| `scam_detection_enabled` | Детектор скама | True |
| `flood_detection_enabled` | Детектор флуда | True |
| `referral_detection_enabled` | Детектор рефералов | False |
| `scam_sensitivity` | Чувствительность (40-90) | 60 |
| `flood_max_repeats` | Макс. повторов для флуда (1-50, ручной ввод ✏️) | 2 |
| `flood_time_window` | Окно флуда в секундах (10-600, ручной ввод ✏️) | 60 |
| `default_action` | Действие по умолчанию | delete |
| `default_mute_duration` | Длительность мута (мин) | 1440 |
| `word_filter_action` | Действие для слов (NULL=default) | NULL |
| `word_filter_mute_duration` | Мут для слов (NULL=default) | NULL |
| `flood_action` | Действие для флуда (NULL=default) | NULL |
| `flood_mute_duration` | Мут для флуда (NULL=default) | NULL |
| `flood_mute_text` | Кастомный текст при муте за флуд | NULL |
| `flood_ban_text` | Кастомный текст при бане за флуд | NULL |
| `flood_warn_text` | Кастомный текст предупреждения за флуд | NULL |
| `flood_detect_any_messages` | Детектировать любые сообщения подряд | False |
| `flood_any_max_messages` | Макс. сообщений для любых | 5 |
| `flood_any_time_window` | Окно для любых сообщений (сек) | 10 |
| `flood_detect_media` | Детектировать медиа-флуд | False |
| `word_filter_normalize` | Нормализатор для слов | True |
| `delete_user_commands` | Удалять команды от обычных пользователей | False |
| `delete_system_messages` | Удалять системные сообщения | False |

#### Настройки категорий слов
| Настройка | Описание | Default |
|-----------|----------|---------|
| `simple_words_enabled` | Простые слова вкл | True |
| `simple_words_action` | Действие для простых | delete |
| `simple_words_mute_duration` | Мут для простых (мин) | NULL |
| `simple_words_mute_text` | Кастомный текст при муте | NULL |
| `simple_words_ban_text` | Кастомный текст при бане | NULL |
| `simple_words_delete_delay` | Задержка удаления сообщения (сек) | NULL |
| `simple_words_notification_delete_delay` | Автоудаление уведомления (сек) | NULL |
| `harmful_words_enabled` | Вредные слова вкл | True |
| `harmful_words_action` | Действие для вредных | ban |
| `harmful_words_mute_duration` | Мут для вредных (мин) | NULL |
| `harmful_words_mute_text` | Кастомный текст при муте | NULL |
| `harmful_words_ban_text` | Кастомный текст при бане | NULL |
| `harmful_words_delete_delay` | Задержка удаления сообщения (сек) | NULL |
| `harmful_words_notification_delete_delay` | Автоудаление уведомления (сек) | NULL |
| `obfuscated_words_enabled` | Обфускация вкл | True |
| `obfuscated_words_action` | Действие для обфускации | mute |
| `obfuscated_words_mute_duration` | Мут для обфускации (мин) | 1440 |
| `obfuscated_words_mute_text` | Кастомный текст при муте | NULL |
| `obfuscated_words_ban_text` | Кастомный текст при бане | NULL |
| `obfuscated_words_delete_delay` | Задержка удаления сообщения (сек) | NULL |
| `obfuscated_words_notification_delete_delay` | Автоудаление уведомления (сек) | NULL |

**Категории слов:**
- **simple** — реклама, спам, обычные запрещённые слова
- **harmful** — наркотики, запрещённый контент
- **obfuscated** — слова с обфускацией (l33tspeak): k0ka, w1шки

**Кастомный текст уведомлений:**
- Поддерживает плейсхолдер `%user%` для упоминания нарушителя
- Пример: `%user% получил мут за спам`
- Если не задан — используется стандартный текст бота

**Задержки:**
- `delete_delay` — через сколько секунд удалить сообщение нарушителя (0 = сразу)
- `notification_delete_delay` — через сколько секунд автоудалить уведомление бота (NULL = не удалять)

#### Структура меню настроек
```
Главное меню Фильтра контента
├── Фильтр слов ✅/❌ + ⚙️ (настройки слов)
├── Антискам ✅/❌ + ⚙️ (настройки антискама: паттерны, чувствительность, действие)
├── Антифлуд ✅/❌ + ⚙️ (настройки флуда: повторы, окно, действие)
├── Referral spam ✅/❌
├── Логирование ✅/❌
└── Модуль вкл/выкл

Настройки фильтра слов (⚙️)
├── 📝 Простые слова (sw) ✅/❌ + ⚙️
├── 💊 Вредные слова (hw) ✅/❌ + ⚙️
├── 🔀 Обфускация (ow) ✅/❌ + ⚙️
└── ◀️ Назад

Действия категории (⚙️)
├── 🗑️ Удалить / 🔇 Мут / 🚫 Бан
├── ⏱️ Время мута (если выбран мут)
├── 📓 Список слов
├── ⚙️ Дополнительно
└── ◀️ Назад

Дополнительные настройки категории (⚙️)
├── 📝 Текст при муте (FSM ввод, поддержка %user%)
├── 📝 Текст при бане (FSM ввод, поддержка %user%)
├── ⏱️ Удаление сообщения (задержка в секундах: 30s, 5min, 1h)
├── 🗑️ Автоудаление уведомления (задержка в секундах)
└── ◀️ Назад

Настройки антифлуда (⚙️)
├── Макс. повторов: 2 / 3 / 5 / ✏️ (ручной ввод 1-50)
├── Окно: 30с / 60с / 120с / ✏️ (ручной ввод 10-600)
├── 📱 Любые сообщения ✅/❌ + ⚙️ (настроить max/window)
├── 🖼️ Медиа-флуд ✅/❌
├── Действие: Удалить / Мут / Бан
├── ⚙️ Дополнительно (текст мута/бана, задержки)
└── ◀️ Назад

Удаление сообщений (🗑️)
├── 🤖 Удалять команды ✅/❌ (команды от обычных пользователей)
├── 📢 Удалять системные ✅/❌ (вступление, выход, закреп)
└── ◀️ Назад

Настройки антискама (⚙️)
├── 🎯 Паттерны (добавить, импорт, список)
├── 📂 Категории сигналов (добавить, редактировать, удалить)
├── Чувствительность: Высокая/Средняя/Низкая
├── Действие: Удалить / Мут / Бан
├── ⚙️ Дополнительно (текст мута/бана, задержки)
├── Логирование ✅/❌
└── ◀️ Назад
```

### Расширенный антифлуд

**Режимы детекции:**
| Режим | Описание | Настройки |
|-------|----------|-----------|
| Одинаковые | Одинаковые сообщения подряд (по умолчанию) | `flood_max_repeats`, `flood_time_window` |
| Любые сообщения | Любые сообщения подряд (не только одинаковые) | `flood_any_max_messages`, `flood_any_time_window` |
| Медиа-флуд | Фото, стикеры, видео, голосовые, видеокружки | Использует настройки "любых сообщений" |

**Типы медиа для детекции:**
- `photo` — фотографии
- `sticker` — стикеры
- `video` — видео
- `voice` — голосовые сообщения
- `video_note` — видеокружки

### Модуль удаления сообщений

**Назначение:** Автоматическое удаление нежелательных сообщений из группы.

**Функции:**
| Функция | Описание |
|---------|----------|
| Удаление команд | Удаляет команды (`/start`, `/help` и т.д.) от обычных пользователей. Команды от админов удаляются, но действие выполняется. |
| Удаление системных | Удаляет системные сообщения (вступление, выход, закреп). |

**Логика работы:**
```
Сообщение в группе
    │
    ├── Проверка: это команда? (начинается с /)
    │   ├── delete_user_commands=True И НЕ админ → удалить, прервать
    │   └── delete_user_commands=True И админ → удалить, продолжить обработку
    │
    └── Проверка: системное сообщение?
        └── delete_system_messages=True → удалить
```

### Redis Keys (Content Filter)
```python
f"cf:flood:{chat_id}:{user_id}:count:{hash}"    # Счётчик одинаковых
f"cf:flood:{chat_id}:{user_id}:any_count"       # Счётчик любых сообщений
f"cf:flood:{chat_id}:{user_id}:media_count"     # Счётчик медиа
f"cf:referral:{chat_id}:{username}:count"       # Счётчик упоминаний @username
f"cf:referral:{chat_id}:{username}:users"       # Set уникальных продвигающих
```

### Модели БД

```python
# Настройки фильтра для группы
class ContentFilterSettings:
    chat_id: BigInteger (PK, FK → groups)
    enabled: Boolean
    word_filter_enabled: Boolean
    scam_detection_enabled: Boolean
    flood_detection_enabled: Boolean
    scam_sensitivity: Integer (40-90)
    default_action: String
    default_mute_duration: Integer
    word_filter_action: String (NULL = use default)
    word_filter_mute_duration: Integer (NULL = use default)
    flood_action: String (NULL = use default)
    flood_mute_duration: Integer (NULL = use default)
    word_filter_normalize: Boolean (True = detect l33tspeak)

# Запрещённые слова
class FilterWord:
    id: Integer (PK)
    chat_id: BigInteger (FK)
    word: String  # Оригинальное слово
    normalized: String  # Нормализованная версия
    match_type: String (word/phrase/regex)
    action: String (None = default)
    action_duration: Integer
    category: String

# Исключения (whitelist)
class FilterWhitelist:
    id: Integer (PK)
    chat_id: BigInteger (FK)
    word: String
    normalized: String

# Лог нарушений
class FilterViolation:
    id: Integer (PK)
    chat_id: BigInteger (FK)
    user_id: BigInteger
    message_id: BigInteger
    detector_type: String
    trigger: String
    action_taken: String
    created_at: DateTime
```

---

## 3.1. Scam Media Filter / Фильтр скам-изображений

### Назначение
Фильтрация скам-изображений по перцептивному хешу (pHash + dHash).
Модуль ловит скам-фото даже при изменении размера, сжатии, добавлении текста.

### Файлы

**Сервисы (services/scam_media/):**
| Файл | Назначение |
|------|------------|
| `services/scam_media/__init__.py` | Экспорт модуля |
| `services/scam_media/db_service.py` | CRUD: SettingsService, BannedHashService, ViolationService |
| `services/scam_media/hash_service.py` | Вычисление pHash/dHash с imagehash |

**Хендлеры (handlers/scam_media/):**
| Файл | Назначение |
|------|------------|
| `handlers/scam_media/__init__.py` | Экспорт check_message_for_scam_media |
| `handlers/scam_media/filter_handler.py` | Проверка входящих сообщений (через coordinator) |
| `handlers/scam_media/commands_handler.py` | Команды /mutein, /banin, /scamrm, /scamlogo |
| `handlers/scam_media/settings_handler.py` | UI настроек в ЛС |
| `handlers/scam_media/callbacks_handler.py` | Callback обработчики кнопок |
| `handlers/scam_media/fsm_handler.py` | FSM загрузки/удаления фото через UI |
| `handlers/scam_media/keyboards.py` | Клавиатуры настроек |

### Команды

| Команда | Описание |
|---------|----------|
| `/mutein` | Добавить фото в базу + замутить отправителя |
| `/banin` | Добавить фото в базу + забанить отправителя |
| `/scamrm` | Удалить хеш из базы (реплай или ID) |
| `/scamlogo` | Добавить область логотипа |

### Действия (ScamMediaAction)

| Действие | Описание |
|----------|----------|
| `delete` | Только удалить сообщение |
| `delete_warn` | Удалить + предупреждение |
| `delete_mute` | Удалить + мут |
| `delete_ban` | Удалить + бан |

### Настройки (ScamMediaSettings)

| Поле | Тип | Default | Описание |
|------|-----|---------|----------|
| `enabled` | Boolean | False | Модуль вкл/выкл |
| `action` | String | "delete_mute" | Действие при совпадении |
| `threshold` | Integer | 10 | Порог расстояния Хэмминга |
| `mute_duration` | Integer | 86400 | Время мута (сек) |
| `ban_duration` | Integer | 0 | Время бана (0=навсегда) |
| `include_global` | Boolean | False | Проверять глобальную базу |
| `log_to_journal` | Boolean | True | Писать в журнал |
| `add_to_scammer_db` | Boolean | True | Добавлять в реестр скаммеров |
| `notification_delete_delay` | Integer | 30 | Авто-удаление уведомлений (сек) |
| `mute_text` | String | None | Текст при муте |
| `ban_text` | String | None | Текст при бане |
| `warn_text` | String | None | Текст предупреждения |

### Pipeline

```
Сообщение с фото
    │
    ▼
group_message_coordinator.py
    │
    ▼
scam_media/filter_handler.py
    │
    ├── extract_photos_from_message()
    │   ├── message.photo
    │   ├── message.document (image/*)
    │   └── message.sticker (static)
    │
    ├── compute_image_hash() → pHash + dHash
    │
    ├── BannedHashService.get_hashes_for_group()
    │   └── Локальные + глобальные (если include_global)
    │
    └── compare_hashes() → Hamming distance
        │
        └── distance <= threshold → MATCH
            │
            ├── Удалить сообщение
            ├── Применить действие (mute/ban)
            ├── Отправить уведомление
            ├── Записать в журнал
            └── Увеличить счётчик matches_count
```

### UI Навигация

```
Настройки группы
    │
    └── Фильтр контента
        │
        └── 🖼️ Скам-изображения
            │
            ├── ⚙️ Настройки
            │   ├── [Вкл/Выкл]
            │   ├── [Действие: ...]
            │   ├── [Порог: ...]
            │   ├── [Время мута: ...]
            │   ├── [Время бана: ...]
            │   └── [⬅️ Назад]
            │
            ├── 📋 Фото
            │   ├── [➕ Добавить фото]
            │   ├── [📋 Список фото]
            │   ├── [🗑️ Удалить по ID]
            │   └── [⬅️ Назад]
            │
            ├── 📝 Тексты
            │   ├── [Текст мута]
            │   ├── [Текст бана]
            │   ├── [Текст предупреждения]
            │   └── [⬅️ Назад]
            │
            └── [⬅️ Назад] → Фильтр контента
```

### Модель БД (BannedImageHash)

```python
class BannedImageHash(Base):
    id: Integer (PK)
    phash: String(16)           # Perceptual hash
    dhash: String(16)           # Difference hash
    chat_id: BigInteger         # NULL = глобальный
    is_global: Boolean          # Для всех групп
    added_by_user_id: BigInteger
    added_by_username: String
    description: String
    logo_region: String         # Область логотипа
    matches_count: Integer      # Счётчик срабатываний
    last_match_at: DateTime
    created_at: DateTime
```

---

## 4. Message Management / Управление сообщениями

### Назначение
Автоматическое управление сообщениями в группе:
- Удаление команд (от админов и пользователей)
- Удаление системных сообщений (вход, выход, закреп, изменение фото)
- Репин (автозакрепление) — защита закреплённого сообщения

### Файлы
| Файл | Назначение |
|------|------------|
| `handlers/message_management/__init__.py` | Модульный роутер |
| `handlers/message_management/settings_handler.py` | UI настроек (callback query) |
| `handlers/message_management/command_handler.py` | Команды `/repin`, `/unrepin` |
| `handlers/message_management/filter_handler.py` | Логика фильтрации (вызывается из координатора) |
| `services/message_management_service.py` | CRUD операции с настройками |
| `keyboards/message_management_keyboards.py` | Клавиатуры |
| `database/models_message_management.py` | Модель данных |

### Настройки (MessageManagementSettings)
| Настройка | Описание | Default |
|-----------|----------|---------|
| `delete_admin_commands` | Удалять команды от админов | False |
| `delete_user_commands` | Удалять команды от пользователей | False |
| `delete_join_messages` | Удалять сообщения о входе | False |
| `delete_leave_messages` | Удалять сообщения о выходе | False |
| `delete_pin_messages` | Удалять уведомления о закрепе | False |
| `delete_chat_photo_messages` | Удалять сообщения об изменении фото/названия | False |
| `repin_enabled` | Включён ли автозакреп | False |
| `repin_message_id` | ID сообщения для автозакрепа | NULL |

### Структура меню
```
📨 Управление сообщениями
├── 🤖 Удаление команд ✅/❌
│   ├── 👑 Админы: ✅/❌
│   └── 👤 Пользователи: ✅/❌
├── 🗨️ Системные сообщения ✅/❌
│   ├── ➡️ Вход участников: ✅/❌
│   ├── ⬅️ Выход участников: ✅/❌
│   ├── 📌 Закреп сообщений: ✅/❌
│   └── 🖼️ Фото/название группы: ✅/❌
├── 📌 Репин (автозакреп) ✅/❌
│   ├── 🔄 Статус: ✅/❌
│   └── 📝 Сообщение: ID или "не задано"
└── 🔙 Назад
```

### Репин (автозакрепление)

**Назначение:** Защита закреплённого сообщения от перезакрепления другими.

**Команды:**
| Команда | Описание |
|---------|----------|
| `/repin` | Установить сообщение для автозакрепа (ответом на сообщение) |
| `/unrepin` | Отключить автозакреп |

**Логика:**
```
Пользователь закрепляет сообщение
    │
    ├── Проверка: от связанного канала? → игнорировать
    │
    ├── Проверка: repin_enabled = True?
    │
    └── Если закреплено другое сообщение:
        └── bot.pin_chat_message(repin_message_id)
```

**Особенности:**
- Закрепы от связанного канала игнорируются
- Команды `/repin` и `/unrepin` доступны только админам
- При включении репина без сообщения — показывается предупреждение

### Pipeline фильтрации

```
Сообщение в группе (group_message_coordinator.py)
    │
    ├── ШАГ 0: MESSAGE MANAGEMENT - системные + репин
    │   ├── is_system_message? → process_system_message()
    │   │   └── Удалить если настроено
    │   └── pinned_message? → process_pin_event()
    │       └── Репин если нужно
    │
    ├── ШАГ 0.5: MESSAGE MANAGEMENT - команды
    │   ├── is_command_message? → process_command_message()
    │   │   ├── is_admin? → проверить delete_admin_commands
    │   │   └── не админ? → проверить delete_user_commands
    │   └── Удалить если настроено
    │
    ├── Проверка: автор - админ? → пропуск CF/AS
    │
    ├── ШАГ 1: Content Filter
    │
    └── ШАГ 2: Antispam
```

### Callback Data формат
```
mm:m:{chat_id}          # Главное меню
mm:cmd:{chat_id}        # Меню удаления команд
mm:sys:{chat_id}        # Меню системных сообщений
mm:repin:{chat_id}      # Меню репина
mm:t:adm:{chat_id}      # Toggle: удаление команд админов
mm:t:usr:{chat_id}      # Toggle: удаление команд пользователей
mm:t:join:{chat_id}     # Toggle: удаление входа
mm:t:leave:{chat_id}    # Toggle: удаление выхода
mm:t:pin:{chat_id}      # Toggle: удаление закрепа
mm:t:photo:{chat_id}    # Toggle: удаление фото/названия
mm:t:repin:{chat_id}    # Toggle: репин вкл/выкл
mm:noop                 # Информационные кнопки (ничего не делают)
```

---

## 5. Mute System / Система мутов

### Назначение
Ограничение пользователей различными способами.

### Виды мутов

#### 5.1 Мут новых участников
**Файлы:**
- `handlers/bot_moderation_handlers/new_member_requested_to_join_mute_handlers.py`
- `services/risk_gate.py`

**Логика:**
```
Новый участник → classify_join_event()
    │
    ├── INVITE → prepare_invite_flow()
    │   └── Антифлуд проверка
    │
    └── evaluate_admission()
        └── risk_gate → оценка риска
            ├── Проверка фото профиля (Pyrogram)
            ├── Проверка возраста аккаунта
            └── Балл риска 0-100
                ├── < 50 → пропуск
                └── >= 50 → мут
```

#### 5.2 Мут по реакции
**Файлы:**
- `handlers/mute_by_reaction/mute_by_reaction_handler.py`
- `services/mute_by_reaction_service/logic.py`

**Логика:**
```
Реакция 🔇 на сообщение
    │
    ├── Проверка: реактор — админ?
    ├── Проверка: автор сообщения — не админ?
    │
    └── restrict_chat_member()
        └── Мут на настраиваемое время
```

#### 5.3 Автомут скаммеров
**Файлы:**
- `handlers/auto_mute_scammers_handlers.py`
- `services/auto_mute_scammers_logic.py`
- `services/enhanced_profile_analyzer.py` — анализ профиля (фото + возраст)
- `services/spammer_registry.py`

**Логика (обновлено 2025-12-19):**
```
Проверка при вступлении (auto_mute_scammer_on_join)
    │
    ├── ПРИОРИТЕТ 1: Флаг автомута из Redis (captcha flow)
    │   └── auto_mute_scammer:{user_id}:{chat_id} = "1" → МУТ
    │
    ├── ПРИОРИТЕТ 2: Уровень скама в БД (scammer_tracker)
    │   └── scam_level >= 50 → МУТ
    │
    └── ПРИОРИТЕТ 3: Анализ профиля (enhanced_profile_analyzer)
        ├── ЕСТЬ фото → проверяем ТОЛЬКО возраст фото
        │   └── ВСЕ фото < 15 дней → МУТ (is_suspicious=True)
        │
        └── НЕТ фото → проверяем возраст аккаунта
            └── Аккаунт < 30 дней → МУТ (is_suspicious=True)
```

**ВАЖНО:** Приоритет возраста фото над возрастом аккаунта!
Это исправляет баг, когда скаммер с аккаунтом 35 дней и фото 0 дней проходил.

### Настройки мутов (ChatSettings)
| Настройка | Описание |
|-----------|----------|
| `mute_new_members` | Мутить новых участников |
| `auto_mute_scammers` | Автомут скаммеров |
| `global_mute_enabled` | Глобальный мут |
| `reaction_mute_enabled` | Мут по реакции |
| `reaction_mute_announce_enabled` | Анонс мута |

#### 5.4 Restriction Service — восстановление мутов после rejoin

**Файлы:**
- `services/restriction_service.py` — сервис сохранения/восстановления ограничений

**Проблема:**
Когда пользователь замучен и выходит из группы, а затем возвращается через `approve_chat_join_request()` (капча или инвайт), Telegram API автоматически снимает все ограничения. Пользователь получает полные права, обходя наказание.

**Решение:**
Сохраняем все муты и баны в таблицу `user_restrictions`. После одобрения join request проверяем БД и восстанавливаем ограничение через Telegram API.

**Функции:**
| Функция | Описание |
|---------|----------|
| `save_restriction()` | Сохраняет мут/бан в БД (создаёт или обновляет) |
| `get_active_restriction()` | Получает активное ограничение (проверяет срок) |
| `deactivate_restriction()` | Деактивирует ограничение (при размуте) |
| `restore_restriction()` | Восстанавливает ограничение через Telegram API |
| `check_and_restore_restriction()` | Комбинация проверки и восстановления |

**Pipeline:**
```
1. Антиспам/ContentFilter/RiskGate мутит пользователя
       │
       ├── bot.restrict_chat_member()  — мут в Telegram
       └── save_restriction()          — сохранение в БД

2. Пользователь выходит из группы
       │
       └── (ограничение остаётся в БД, is_active=True)

3. Пользователь заходит через капчу
       │
       ├── approve_chat_join_request() — Telegram снимает мут!
       │
       └── check_and_restore_restriction()
           ├── get_active_restriction() — ищем в БД
           └── restore_restriction()    — восстанавливаем мут
```

**Интеграция:**
Сохранение вызывается из:
- `handlers/antispam_handlers/antispam_filter_handler.py` — после применения мута
- `handlers/content_filter/filter_handler.py` — после применения мута/бана
- `services/auto_mute_scammers_logic.py` — при автомуте скаммеров

Восстановление вызывается из:
- `services/visual_captcha_logic.py` — после `approve_chat_join_request()`

---

## 6. Settings / Настройки

### Назначение
Управление настройками групп через ЛС бота.

### Файлы
| Файл | Назначение |
|------|------------|
| `handlers/group_settings_handler/groups_settings_in_private_handler.py` | /settings команда |
| `services/groups_settings_in_private_logic.py` | Бизнес-логика |
| `keyboards/group_settings_kb.py` | Клавиатуры |

### Flow

```
/settings в ЛС
    │
    ├── get_admin_groups() → список групп
    │
    └── Показать список групп с кнопками

Выбор группы
    │
    ├── check_granular_permissions() → проверка прав
    │
    └── Показать меню настроек

Переключение настройки
    │
    ├── Обновление в БД
    └── Обновление клавиатуры
```

### Проверка прав
```python
async def check_granular_permissions(bot, user_id, chat_id, permission, session):
    """
    Проверяет конкретное право пользователя:
    - can_delete_messages
    - can_restrict_members
    - can_change_info
    - и т.д.
    """
```

---

## 7. Journal / Журнал активности

### Назначение
Логирование событий группы в отдельный канал/группу.

### Файлы
| Файл | Назначение |
|------|------------|
| `handlers/journal_link_handler.py` | Привязка журнала |
| `handlers/bot_activity_journal/bot_activity_journal.py` | Отправка логов |
| `services/bot_activity_journal/bot_activity_journal_logic.py` | Логика |
| `services/group_journal_service.py` | Работа с БД |

### Типы событий
- Новый участник
- Участник вышел/кикнут
- Прохождение капчи
- Таймаут капчи
- Срабатывание антиспам
- Мут/размут

### Привязка журнала
```
/linkjournal → инструкции
    │
Пересылка сообщения из канала журнала
    │
    ├── IsAdminWithChangeInfoFilter → проверка прав
    ├── link_journal_channel() → сохранение в БД
    └── Подтверждение
```

---

## 8. Broadcast / Рассылки

### Назначение
Массовая отправка сообщений пользователям.

### Файлы
| Файл | Назначение |
|------|------------|
| `handlers/broadcast_handlers/broadcast_handlers.py` | Команды рассылки |
| `services/broadcast_logic.py` | Логика отправки |

### Flow
```
/broadcast
    │
    ├── Проверка: это владелец бота?
    ├── Получение списка пользователей
    │
    └── Цикл отправки с задержкой
        ├── Успех → счётчик++
        └── Ошибка → логирование
```

---

## 9. Risk Assessment / Оценка риска

### Назначение
Определение подозрительных пользователей.

### Файлы
| Файл | Назначение |
|------|------------|
| `services/risk_gate.py` | Основная логика (для капчи) |
| `services/enhanced_profile_analyzer.py` | Расширенный анализ (для автомута) |
| `services/account_age_estimator.py` | Оценка возраста аккаунта по user_id |
| `services/pyrogram_client.py` | MTProto клиент (даты фото) |

### Логика приоритета (обновлено 2025-12-19)

**enhanced_profile_analyzer.py** используется в `auto_mute_scammer_on_join()`:

```
analyze_user_profile_enhanced()
    │
    ├── ЕСТЬ фото?
    │   ├── ДА → проверяем ТОЛЬКО возраст фото (ПРИОРИТЕТ!)
    │   │   └── ВСЕ фото < 15 дней → is_suspicious=True, risk_score=100
    │   │   └── Хотя бы одно фото >= 15 дней → is_suspicious=False
    │   │
    │   └── Возраст аккаунта НЕ учитывается если есть фото!
    │
    └── НЕТ фото?
        └── Проверяем возраст аккаунта (ЗАПАСНОЙ)
            └── Аккаунт < 30 дней → is_suspicious=True, risk_score=100
```

### Факторы риска

| Фактор | Условие | Результат |
|--------|---------|-----------|
| ВСЕ фото < 15 дней | Есть фото | is_suspicious=True (100 баллов) |
| Аккаунт < 30 дней | Нет фото | is_suspicious=True (100 баллов) |
| В реестре спаммеров | scam_level >= 50 | Мут (приоритет 2) |
| Флаг автомута в Redis | auto_mute_scammer:{user_id} | Мут (приоритет 1) |

### Порог
- `is_suspicious = False` → пропуск
- `is_suspicious = True` → мут

### Динамический расчёт возраста аккаунта

**Формула:**
```
age_days = (max_seen_id - user_id) / DAILY_GROWTH
```

**Параметры:**
| Параметр | Значение | Описание |
|----------|----------|----------|
| `DAILY_GROWTH` | 2,500,000 | Прирост user_id в день |
| `max_seen_id` | Redis: `telegram:max_seen_user_id` | Максимальный известный ID |
| Fallback | 8,600,000,000 | Если Redis пустой |

**Как работает:**
1. При каждом join_request вызывается `update_max_seen_id()`
2. Если новый ID > текущего max → обновляем Redis
3. При расчёте возраста: `(max - user_id) / 2.5M = дни`

**Методы в `account_age_estimator.py`:**
- `get_dynamic_age_days(redis, user_id)` — расчёт возраста
- `update_max_seen_id(redis, user_id)` — обновление max
- `is_young_account_dynamic(redis, user_id, threshold)` — проверка

---

## 10. Profile Monitor / Мониторинг профилей

### Назначение
Отслеживание изменений профиля пользователей ПОСЛЕ вступления в группу:
- Логирование изменений имени, username, фото
- Автоматический мут подозрительных пользователей
- Уведомления в журнал группы с кнопками действий

### Файлы
| Файл | Назначение |
|------|------------|
| `handlers/profile_monitor/__init__.py` | Модульный роутер |
| `handlers/profile_monitor/monitor_handler.py` | Обработка сообщений (вызывается из координатора) |
| `handlers/profile_monitor/callbacks_handler.py` | Кнопки действий (Мут/Бан/Кик/В группу/ОК) |
| `handlers/profile_monitor/settings_handler.py` | UI настроек |
| `services/profile_monitor/profile_monitor_service.py` | Бизнес-логика |
| `keyboards/profile_monitor_kb.py` | Клавиатуры |
| `database/models_profile_monitor.py` | Модели данных |

### Критерии автомута

**Критерий 1: Смена имени + смена фото**
```
Смена имени в течение 24ч + смена фото в течение 24ч + сообщение в течение 30 мин
    → Мут навсегда + удаление ВСЕХ сообщений пользователя
```

**Критерий 2: Смена имени + быстрые сообщения**
```
Смена имени в течение 24ч + сообщение в течение 30 мин после смены
    → Мут навсегда + удаление ВСЕХ сообщений пользователя
```

**Критерий 3: Добавление фото + быстрые сообщения**
```
Добавление фото (не было → стало) + сообщение в течение 30 мин после добавления
    → Мут навсегда + удаление ВСЕХ сообщений пользователя
```
Работает только для пользователей БЕЗ фото при входе.

**Критерий 4: Свежее фото + смена имени + быстрые сообщения (НОВЫЙ 2025-12-21)**
```
Свежее фото (<N дней) + смена имени в течение 24ч + сообщение в течение 30 мин
    → Мут навсегда + удаление ВСЕХ сообщений пользователя
```
Порог свежести фото настраивается: 1 день (по умолчанию), 3, 7 или 14 дней.

**Критерий 5: Свежее фото + быстрые сообщения (НОВЫЙ 2025-12-21)**
```
Свежее фото (<N дней) + сообщение в течение 30 мин после входа
    → Мут навсегда + удаление ВСЕХ сообщений пользователя
```
Срабатывает если ВСЕ фото профиля свежее порога.

**Приоритет:** Если есть фото → проверяется свежесть фото (критерии 4,5). Если нет фото → проверяется возраст аккаунта (критерий 3).

Все критерии работают независимо. Критерий 1 самый строгий (имя + фото), критерии 4,5 — для обнаружения спаммеров со свежими фото.

### Отправка уведомлений

**В журнал (для админов):** Подробные уведомления с кнопками действий (Мут/Бан/Кик/В группу/ОК).

**В группу (для всех участников):** Простые уведомления при смене имени:
```
📝 Пользователь сменил имя

Было: Иван Иванов
Стало: John Doe

📋 История имён:
• Иван Иванов (01.01.2025)
• John Doe (13.12.2025)
```
Опция включается в настройках (по умолчанию выключена).

### Создание снапшота профиля

**Основной сценарий (обновлено 2025-12-18):**
```
Пользователь вступает в группу (chat_member_updated)
    │
    └── new_member_requested_to_join_mute_handlers.py: MANUAL_MUTE_HANDLER()
        │
        ├── Детект: LEFT/KICKED → MEMBER
        │
        └── create_snapshot_on_join()
            ├── Получение данных профиля (Pyrogram)
            ├── has_photo, account_age_days
            └── Сохранение снапшота с joined_at = NOW
                (При rejoin — обновление joined_at на текущее время)
```

**FALLBACK сценарий:**
Если снапшот не был создан при входе (пользователь вступил до включения модуля),
снапшот создаётся при первом сообщении в `monitor_handler.py`.

### Pipeline мониторинга

```
Сообщение в группе (через group_message_coordinator)
    │
    ├── ШАГ 3: PROFILE MONITOR
    │
    ├── Загрузка настроек модуля
    │   └── enabled = False? → выход
    │
    ├── Снапшот существует?
    │   └── НЕТ → _handle_first_message() [FALLBACK]
    │       ├── Создание снапшота (joined_at = now)
    │       └── Проверка автомута
    │
    └── Снапшот существует?
        └── ДА → check_profile_changes()
            ├── Получение текущих данных профиля
            ├── Сравнение со снапшотом
            │
            ├── Изменения найдены?
            │   ├── ДА → log_profile_change()
            │   │   ├── Сохранение в profile_change_logs
            │   │   └── send_journal_notification()
            │   │
            │   └── Проверка автомута
            │       ├── Смена имени < 24ч?
            │       ├── Смена фото < 24ч? (критерий 1)
            │       └── Сообщение < 20 мин после смены?
            │           → apply_auto_mute()
            │
            └── Обновление снапшота
```

### Уведомления в журнал

**При обнаружении изменения:**
```
📊 Изменение профиля

👤 Пользователь: @username (123456789)
📝 Тип: Смена имени
📅 Время: 2025-01-01 12:30:45

Было: Иван Иванов
Стало: John Doe

[🔇 Мут] [🚫 Бан] [👢 Кик]
[📢 В группу] [✅ ОК]
```

**После автомута:**
```
🔒 Автомут: смена имени

👤 Пользователь: @username (123456789)
📝 Причина: Смена имени + сообщение в течение 20 мин
📅 Время: 2025-01-01 12:30:45
🗑️ Удалено сообщений: 5

[🔊 Размут] [🚫 Бан]
[📢 В группу] [✅ ОК]
```

### Кнопки действий

| Кнопка | Действие |
|--------|----------|
| 🔇 Мут | Мут навсегда |
| 🚫 Бан | Бан навсегда |
| 👢 Кик | Исключение (без бана) |
| 🔊 Размут | Снять ограничения |
| 📢 В группу | Отправить историю изменений в группу |
| ✅ ОК | Закрыть уведомление |

### Настройки (ProfileMonitorSettings)

| Настройка | Описание | Default |
|-----------|----------|---------|
| `enabled` | Модуль включён | False |
| `log_name_changes` | Логировать изменения имени | True |
| `log_username_changes` | Логировать изменения username | True |
| `log_photo_changes` | Логировать изменения фото | True |
| `send_to_journal` | Отправлять уведомления в журнал | True |
| `auto_mute_young_no_photo` | Автомут: молодой аккаунт без фото | True |
| `auto_mute_name_change` | Автомут: смена имени + быстрые сообщения | True |
| `delete_messages_on_mute` | Удалять все сообщения при автомуте | True |
| `account_age_threshold_days` | Порог возраста аккаунта (дни) | 15 |
| `photo_freshness_threshold_days` | Порог свежести фото (дни) для критериев 4,5 | 1 |
| `name_change_window_hours` | Окно отслеживания смены имени (часы) | 24 |
| `first_message_window_minutes` | Окно "быстрых сообщений" (минуты) | 30 |

### Структура меню настроек

```
📊 Мониторинг профилей
├── 🟢/🔴 Включить/Выключить
├── 📝 Настройки логирования
│   ├── ✅/❌ Имена
│   ├── ✅/❌ Username
│   ├── ✅/❌ Фото
│   └── ✅/❌ Отправка в журнал
├── ⚡ Настройки автомута
│   ├── ✅/❌ Мут: нет фото + молодой аккаунт
│   ├── 📅 Порог аккаунта: 15 дней (7/15/30/60/90)
│   ├── 📸 Порог фото: 1 день (1/3/7/14)
│   ├── ✅/❌ Мут: смена имени + быстрые сообщения
│   └── ✅/❌ Удалять сообщения спаммеров
└── ◀️ Назад
```

### Callback Data формат

```
pm_settings_main:{chat_id}           # Главное меню настроек
pm_toggle:{on|off}:{chat_id}         # Вкл/выкл модуля
pm_settings_log:{chat_id}            # Меню логирования
pm_settings_mute:{chat_id}           # Меню автомута
pm_log_name:{on|off}:{chat_id}       # Toggle: логирование имён
pm_log_username:{on|off}:{chat_id}   # Toggle: логирование username
pm_log_photo:{on|off}:{chat_id}      # Toggle: логирование фото
pm_send_journal:{on|off}:{chat_id}   # Toggle: отправка в журнал
pm_mute_young:{on|off}:{chat_id}     # Toggle: автомут молодых
pm_mute_name:{on|off}:{chat_id}      # Toggle: автомут при смене имени
pm_delete_msgs:{on|off}:{chat_id}    # Toggle: удаление сообщений
pm_age_threshold:{chat_id}           # Меню выбора порога возраста аккаунта
pm_set_age:{days}:{chat_id}          # Установка порога возраста
pm_photo_fresh_threshold:{chat_id}   # Меню выбора порога свежести фото
pm_set_photo_fresh:{days}:{chat_id}  # Установка порога свежести фото

# Кнопки действий в журнале
pm_mute:{chat_id}:{user_id}:{log_id}       # Мут
pm_ban:{chat_id}:{user_id}:{log_id}        # Бан
pm_kick:{chat_id}:{user_id}:{log_id}       # Кик
pm_unmute:{chat_id}:{user_id}:{log_id}     # Размут
pm_send_group:{chat_id}:{user_id}:{log_id} # Отправить в группу
pm_ok:{chat_id}:{user_id}:{log_id}         # ОК (закрыть)
```

### Модели БД

```python
# Настройки модуля для группы
class ProfileMonitorSettings:
    chat_id: BigInteger (PK, FK → groups)
    enabled: Boolean
    log_name_changes: Boolean
    log_username_changes: Boolean
    log_photo_changes: Boolean
    send_to_journal: Boolean
    auto_mute_young_no_photo: Boolean
    auto_mute_name_change: Boolean
    delete_messages_on_mute: Boolean
    account_age_threshold_days: Integer (default: 15)
    photo_freshness_threshold_days: Integer (default: 1)
    name_change_window_hours: Integer (default: 24)
    first_message_window_minutes: Integer (default: 30)

# Снапшот профиля пользователя при первом сообщении
class ProfileSnapshot:
    id: Integer (PK)
    chat_id: BigInteger (FK → groups)
    user_id: BigInteger
    first_name: String
    last_name: String (nullable)
    username: String (nullable)
    has_photo: Boolean
    photo_file_id: String (nullable)
    first_message_at: DateTime
    created_at: DateTime
    updated_at: DateTime

# Лог изменений профиля
class ProfileChangeLog:
    id: Integer (PK)
    chat_id: BigInteger (FK → groups)
    user_id: BigInteger
    change_type: String (name/username/photo)
    old_value: String (nullable)
    new_value: String (nullable)
    detected_at: DateTime
    auto_action_taken: String (nullable)
    manual_action_taken: String (nullable)
    manual_action_by: BigInteger (nullable)
    manual_action_at: DateTime (nullable)
    journal_message_sent: Boolean
```

### Интеграция

**Вызов из координатора:**
```python
# bot/handlers/group_message_coordinator.py
# После ContentFilter и Antispam

# ШАГ 3: PROFILE MONITOR
await _process_profile_monitor(message, session)
```

**Кнопка в меню настроек:**
```python
# bot/handlers/group_settings_handler/groups_settings_in_private_handler.py
InlineKeyboardButton(
    text="📊 Мониторинг профилей",
    callback_data=f"pm_settings_main:{chat_id}"
)
```

---

## 11. Group Auto Sync / Автосинхронизация

### Назначение
Автоматическое создание групп и админов в БД.

### Файлы
| Файл | Назначение |
|------|------------|
| `middleware/group_auto_sync_middleware.py` | Middleware |
| `services/group_auto_sync.py` | Бизнес-логика |

### Логика
```
Любое событие из группы
    │
    ├── _is_recently_synced() → проверка кэша
    │   └── Если в кэше → пропуск
    │
    ├── ensure_group_exists()
    │   ├── Проверка группы в БД
    │   ├── Создание если нет
    │   └── Обновление title если изменился
    │
    ├── sync_group_admins()
    │   ├── bot.get_chat_administrators()
    │   ├── Создание User записей
    │   └── Создание UserGroup связей
    │
    └── _mark_as_synced() → Redis TTL 5 мин
```

---

## 12. User Statistics / Статистика пользователей

### Назначение
Команда `/stat` для просмотра статистики пользователя в группе.
Доступна только администраторам и создателю группы.

### Использование
```
/stat           — по реплаю на сообщение
/stat 123456789 — по ID пользователя
```

### Выводимая информация
- Первый вход в группу
- Возраст аккаунта Telegram
- Premium статус
- Наличие фото профиля
- Количество сообщений
- Дней активности
- Последнее сообщение
- Количество нарушений

### Файлы
| Файл | Назначение |
|------|------------|
| `handlers/user_stats_handler.py` | Хендлер команды /stat |
| `services/user_stats_service.py` | Сервис инкремента статистики |
| `database/models_user_stats.py` | Модель UserStatistics |

### Логика
```
/stat в группе
    │
    ├── Проверка прав админа
    │   └── Если не админ → отказ
    │
    ├── Получение user_id
    │   ├── Из реплая
    │   └── Из аргумента команды
    │
    ├── Сбор данных
    │   ├── profile_snapshots → joined_at, account_age
    │   ├── user_statistics → message_count, active_days
    │   └── filter_violations → COUNT нарушений
    │
    ├── Отправка в ЛС админу
    │   └── Если не может → кнопка "Начать диалог"
    │
    └── Уведомление в группе (удаляется через 5 сек)
```

### Таблица user_statistics
| Колонка | Тип | Описание |
|---------|-----|----------|
| id | INTEGER PK | Автоинкремент |
| chat_id | BIGINT | ID группы |
| user_id | BIGINT | ID пользователя |
| message_count | INTEGER | Количество сообщений |
| last_message_at | DATETIME | Время последнего сообщения |
| active_days | INTEGER | Уникальные дни с сообщениями |
| last_active_date | DATETIME | Дата последнего дня активности |

**Уникальный ключ:** `(chat_id, user_id)`

### Инкремент счётчиков
Счётчики обновляются в `group_message_coordinator.py` при каждом сообщении:
- `message_count += 1`
- `last_message_at = NOW()`
- `active_days += 1` если `last_active_date != today`

---

---

## 13. HTML Utils / Утилиты для HTML

### Назначение
Валидация и экранирование HTML для Telegram сообщений.
Предотвращает ошибки `TelegramBadRequest: Unsupported start tag`.

### Файлы
| Файл | Назначение |
|------|------------|
| `bot/utils/html_utils.py` | Валидация и экранирование HTML |
| `tests/unit/test_html_utils.py` | Unit-тесты (20 тестов) |
| `tests/integration/test_telegram_html.py` | Интеграционные тесты с реальным Telegram API |

### Функции

| Функция | Описание |
|---------|----------|
| `validate_telegram_html(text)` | Проверяет корректность HTML, возвращает `(is_valid, errors)` |
| `escape_html(text)` | Экранирует `<`, `>`, `&` в `&lt;`, `&gt;`, `&amp;` |
| `safe_format_html(template, **kwargs)` | Форматирует шаблон с авто-экранированием значений |
| `validate_and_log(text, context)` | Валидирует и логирует ошибки |

### Проблема и решение

**Проблема:** Telegram парсит HTML и выбрасывает ошибку на некорректные теги.
```python
# Это вызовет TelegramBadRequest:
text = "Порог: <5 дней"  # <5 интерпретируется как тег
```

**Решение:**
```python
from bot.utils.html_utils import escape_html, validate_telegram_html

# Вариант 1: экранирование
text = f"Порог: {escape_html('<5')} дней"  # "Порог: &lt;5 дней"

# Вариант 2: ручное экранирование в шаблоне
text = f"Порог: &lt;{threshold} дней"

# Вариант 3: валидация перед отправкой
is_valid, errors = validate_telegram_html(text)
if not is_valid:
    logger.error(f"HTML errors: {errors}")
```

### Разрешённые теги Telegram
```
<b>, <strong>     — жирный
<i>, <em>         — курсив
<u>, <ins>        — подчёркнутый
<s>, <strike>, <del> — зачёркнутый
<span>, <tg-spoiler> — spoiler
<a href="">       — ссылки
<code>, <pre>     — код
<blockquote>      — цитата
<tg-emoji>        — кастомные эмодзи
```

### Интеграционные тесты

Тесты отправляют реальные сообщения в Telegram для проверки HTML:

```bash
# Запуск интеграционных тестов
pytest tests/integration/test_telegram_html.py -v
```

**Требования:**
- `.env.test` с `TEST_BOT_TOKEN` и `TEST_CHAT_ID`
- Бот должен быть админом в тестовой группе

**Что проверяется:**
- Базовые HTML теги работают
- Экранированные символы отображаются корректно
- Неэкранированный `<` с цифрой вызывает ошибку
- Реальные тексты из бота (настройки автомута) работают

---

## 14. Settings Export/Import / Экспорт и импорт настроек

### Назначение
Экспорт всех настроек группы в JSON файл и импорт настроек из файла в другую группу.
Позволяет быстро переносить конфигурацию между группами.

### Доступ
**Только для:**
- Владельца группы (creator)
- Администратора со ВСЕМИ правами

### Команды
| Команда | Описание |
|---------|----------|
| `/export_settings` | Экспортировать настройки группы в JSON файл |
| `/import_settings` | Импортировать настройки из JSON файла |

### Файлы

**Сервисы (services/settings_export/):**
| Файл | Назначение |
|------|------------|
| `services/settings_export/__init__.py` | Экспорт модуля |
| `services/settings_export/export_service.py` | Логика экспорта/импорта, TABLE_REGISTRY |
| `services/settings_export/permissions.py` | Проверка прав (owner / full admin) |

**Хендлеры (handlers/settings_export/):**
| Файл | Назначение |
|------|------------|
| `handlers/settings_export/__init__.py` | Экспорт роутера |
| `handlers/settings_export/export_handlers.py` | Команда /export_settings, callbacks |
| `handlers/settings_export/import_handlers.py` | Команда /import_settings, FSM |

**Клавиатуры:**
| Файл | Назначение |
|------|------------|
| `keyboards/settings_export_kb.py` | Клавиатуры выбора группы, подтверждения |

### Экспортируемые таблицы (TABLE_REGISTRY)

| Таблица | Описание |
|---------|----------|
| `chat_settings` | Основные настройки группы (капча, муты, фильтры) |
| `captcha_settings` | Настройки капчи |
| `content_filter_settings` | Настройки фильтра контента |
| `profile_monitor_settings` | Настройки мониторинга профилей |
| `filter_words` | Запрещённые слова |
| `filter_whitelist` | Белый список слов |
| `scam_patterns` | Паттерны скама |
| `scam_signal_categories` | Категории сигналов скама |
| `antispam_rules` | Правила антиспама |
| `antispam_whitelist` | Белый список антиспама |

### Расширяемость

Для добавления новой таблицы в экспорт:
```python
# services/settings_export/export_service.py
TABLE_REGISTRY.append(TableConfig(
    model=NewModel,
    key_name='new_model',
    chat_id_column='chat_id',
    is_settings=True,  # или False для data-таблиц
))
```

### Pipeline экспорта

```
/export_settings
    │
    ├── Проверка базового доступа (ALLOWED_USER_IDS)
    │
    ├── get_admin_groups() → список групп пользователя
    │
    ├── can_export_import_settings() → фильтрация групп
    │   └── Только owner или full admin
    │
    ├── Выбор группы → callback export_select:{chat_id}
    │
    └── Подтверждение → callback export_confirm:{chat_id}
        │
        ├── export_group_settings(session, chat_id)
        │   ├── Чтение всех таблиц из TABLE_REGISTRY
        │   └── Сериализация в словарь
        │
        ├── serialize_settings_to_json() → JSON строка
        │
        └── Отправка файла в ЛС
```

### Pipeline импорта

```
/import_settings
    │
    ├── Проверка доступа
    │
    ├── Выбор группы → callback import_select:{chat_id}
    │
    ├── FSM: ImportSettingsStates.waiting_for_file
    │   └── Ожидание JSON файла
    │
    ├── Загрузка файла
    │   ├── Проверка формата (.json)
    │   ├── Проверка размера (< 1 МБ)
    │   ├── deserialize_settings_from_json()
    │   └── validate_import_data() → проверка структуры
    │
    ├── Подтверждение → callback import_confirm:{chat_id}
    │
    └── import_group_settings(session, chat_id, data, user_id)
        ├── Удаление старых записей (если не merge)
        ├── Создание новых записей из JSON
        └── Commit
```

### Формат JSON файла

```json
{
  "export_version": "1.0",
  "exported_at": "2025-12-21T15:30:00",
  "source_chat_id": -1001234567890,
  "data": {
    "chat_settings": {
      "captcha_join_enabled": true,
      "captcha_timeout_seconds": 300,
      ...
    },
    "filter_words": [
      {"word": "спам", "normalized": "спам", "match_type": "word", "category": "simple"},
      ...
    ],
    "antispam_rules": [
      {"rule_type": "TELEGRAM_LINK", "action": "DELETE", "delete_message": true},
      ...
    ],
    ...
  }
}
```

### Проверка прав (permissions.py)

**Требуемые права для full admin:**
- `can_manage_chat`
- `can_delete_messages`
- `can_restrict_members`
- `can_promote_members`
- `can_change_info`
- `can_invite_users`

**Все права должны быть включены!**

### Callback Data формат

```
export_select:{chat_id}    # Выбор группы для экспорта
export_confirm:{chat_id}   # Подтверждение экспорта
export_cancel              # Отмена экспорта

import_select:{chat_id}    # Выбор группы для импорта
import_confirm:{chat_id}   # Подтверждение импорта
import_cancel              # Отмена импорта
```

### FSM States

```python
class ImportSettingsStates(StatesGroup):
    waiting_for_file = State()  # Ожидание JSON файла
```

---

## 15. Cross-Group Detection / Кросс-групповая детекция скамеров

### Назначение
Автоматическая детекция скамеров, которые входят в несколько групп бота и рассылают спам.
При срабатывании детекции бот автоматически применяет действие (мут/бан/кик) во ВСЕХ группах,
где был обнаружен пользователь.

### Критерии детекции (все должны выполниться)

1. **Вход в несколько групп** — пользователь вошёл в N+ групп в течение заданного интервала
2. **Смена профиля** — пользователь изменил имя или фото в течение заданного окна
3. **Сообщения в группах** — пользователь написал сообщения в 2+ группах в течение интервала

### Доступ
**Только для:** ADMIN_IDS из конфигурации

### Файлы

**Модели (database/):**
| Файл | Назначение |
|------|------------|
| `database/models_cross_group.py` | Модели: CrossGroupScammerSettings, CrossGroupUserActivity, CrossGroupDetectionLog |

**Сервисы (services/cross_group/):**
| Файл | Назначение |
|------|------------|
| `services/cross_group/__init__.py` | Экспорт модуля |
| `services/cross_group/settings_service.py` | CRUD настроек, format_seconds_to_human |
| `services/cross_group/detection_service.py` | track_user_join, track_profile_change, track_user_message, check_cross_group_detection |
| `services/cross_group/action_service.py` | apply_cross_group_action, send_journal_notification |

**Хендлеры (handlers/cross_group/):**
| Файл | Назначение |
|------|------------|
| `handlers/cross_group/__init__.py` | Экспорт роутера |
| `handlers/cross_group/settings_handler.py` | UI настроек (callbacks cg:*) |
| `handlers/cross_group/callbacks_handler.py` | Кнопки в журнале (unmute, mute7d, ban, delete, ok) |

**Клавиатуры:**
| Файл | Назначение |
|------|------------|
| `keyboards/cross_group_kb.py` | Клавиатуры настроек и журнала |

### Точки интеграции

Трекинг вызывается из существующих модулей:

| Событие | Где вызывается | Функция |
|---------|----------------|---------|
| Вход в группу | `handlers/profile_monitor/join_handler.py` | `track_user_join()` |
| Смена профиля | `handlers/profile_monitor/monitor_handler.py` | `track_profile_change()` |
| Сообщение | `handlers/group_message_coordinator.py` | `track_user_message()` |

### Настройки (CrossGroupScammerSettings)

| Настройка | Описание | Default |
|-----------|----------|---------|
| `enabled` | Модуль включён | True |
| `join_interval_seconds` | Интервал для входов | 86400 (24ч) |
| `profile_change_window_seconds` | Окно для смены профиля | 21600 (6ч) |
| `message_interval_seconds` | Интервал для сообщений | 3600 (1ч) |
| `min_groups` | Минимум групп для детекции | 2 |
| `action_type` | Действие (mute/ban/kick/delete) | MUTE |
| `mute_duration_seconds` | Длительность мута | 604800 (7д) |
| `excluded_groups` | Исключённые группы (JSONB) | [] |

### Callback Data формат

```
cg:main                    # Главное меню настроек
cg:toggle                  # Вкл/выкл модуль
cg:set:join_interval       # Выбор интервала входов
cg:set:profile_window      # Выбор окна смены профиля
cg:set:message_interval    # Выбор интервала сообщений
cg:set:min_groups          # Выбор мин. групп
cg:set:action              # Выбор действия
cg:set_value:{name}:{val}  # Установка значения
cg:exclusions              # Управление исключениями
cg:add_exclusion           # Добавить исключение
cg:add_exclusion:{chat_id} # Добавить конкретную группу
cg:remove_exclusion:{id}   # Удалить исключение
cg:back_to_main            # Назад к главному меню

# Кнопки в журнале:
cg:unmute:{user_id}:{chat_id}    # Размут
cg:mute7d:{user_id}:{chat_id}    # Мут на 7 дней
cg:ban:{user_id}:{chat_id}       # Бан
cg:delete:{user_id}:{chat_id}    # Удаление (TODO: Pyrogram)
cg:ok:{user_id}:{chat_id}        # Закрыть (убрать кнопки)
```

### Pipeline детекции

```
Событие (вход/смена профиля/сообщение)
    │
    ├── track_*() → запись активности в БД
    │   └── CrossGroupUserActivity (user_id, chat_id, event_type, timestamp)
    │
    └── check_cross_group_detection()
        │
        ├── Загрузка настроек (get_cross_group_settings)
        │   └── Если disabled → return None
        │
        ├── Проверка критерия 1: join_count >= min_groups
        │   └── SELECT DISTINCT chat_id WHERE event='join' AND timestamp > now - interval
        │
        ├── Проверка критерия 2: has_profile_change
        │   └── SELECT 1 WHERE event='profile_change' AND timestamp > now - window
        │
        ├── Проверка критерия 3: message_groups >= 2
        │   └── SELECT DISTINCT chat_id WHERE event='message' AND timestamp > now - interval
        │
        └── Если все критерии выполнены:
            │
            ├── return detection_data {groups: [...], reason: ...}
            │
            └── apply_cross_group_action()
                │
                ├── Для каждой группы:
                │   ├── Проверка excluded_groups
                │   └── Применение action (mute/ban/kick)
                │
                ├── Логирование в CrossGroupDetectionLog
                │
                └── send_journal_notification()
                    └── Отправка в журнал каждой группы с кнопками
```

### Журнал (формат сообщения)

```
⚠️ Кросс-групповой скамер

👤 Пользователь: @username [123456789]
🔢 Затронутые группы: 3
📋 Группы: Group A, Group B, Group C
⏱️ Интервал входов: 2 часа
📝 Смена профиля: имя изменено
💬 Сообщения в группах: 3

⚡ Действие: Мут на 7 дней

[Размут] [Мут 7д] [Бан] [OK]
```

### Миграция

```bash
# Создание таблиц
alembic upgrade d0e1f2g3h4i5
```

Миграция создаёт:
- Таблицу `cross_group_scammer_settings` (singleton, id=1)
- Таблицу `cross_group_user_activity` (индексы по user_id, chat_id, event_type)
- Таблицу `cross_group_detection_logs` (история детекций)
- ENUM типы: `cross_group_action_type`, `profile_change_type`

---

## 13. Anti-Raid / Защита от массовых атак (NEW 2026-01)

### Назначение
Защита группы от различных типов массовых атак и злоупотреблений.

### Компоненты модуля

| Компонент | Описание | Действие |
|-----------|----------|----------|
| **Join/Exit Abuse** | Боты входят-выходят чтобы засветить имя | ban/kick/mute |
| **Name Pattern Ban** | Бан по паттернам имени при входе | ban/kick |
| **Mass Join (Raid)** | Много вступлений одновременно | slowmode/lock/notify |
| **Mass Invite** | Один юзер приглашает много людей | warn/kick/ban |
| **Mass Reaction** | Спам реакциями | warn/mute/kick |

### Файлы

**Модели (database/):**
| Файл | Назначение |
|------|------------|
| `models_antiraid.py` | AntiRaidSettings, AntiRaidNamePattern |

**Сервисы (services/antiraid/):**
| Файл | Назначение |
|------|------------|
| `__init__.py` | Экспорт модуля |
| `settings_service.py` | CRUD настроек и паттернов |
| `name_pattern_checker.py` | Проверка имени по паттернам (TextNormalizer) ✅ |
| `action_service.py` | Применение действий (ban/kick/mute) ✅ |
| `journal_service.py` | Отправка в журнал группы ✅ |
| `join_exit_tracker.py` | Redis трекинг входов/выходов ✅ |
| `mass_join_tracker.py` | Redis трекинг рейда (TODO) |
| `mass_invite_tracker.py` | Redis трекинг инвайтов (TODO) |
| `mass_reaction_tracker.py` | Redis трекинг реакций (TODO) |

**Хендлеры (handlers/antiraid/):**
| Файл | Назначение |
|------|------------|
| `__init__.py` | Экспорт роутеров ✅ |
| `journal_callbacks.py` | Callbacks кнопок журнала (unban, ok, permban) ✅ |
| `join_exit_handler.py` | Обработка выходов (LEFT_TRANSITION) ✅ |
| `settings_handler.py` | UI настроек в ЛС (TODO) |
| `patterns_handler.py` | UI паттернов имён (TODO) |

### Дефолтные значения (настраиваемые через UI)

| Компонент | Параметр | Дефолт |
|-----------|----------|--------|
| Join/Exit | окно | 60 сек |
| Join/Exit | порог | 3 события |
| Join/Exit | действие | ban |
| Join/Exit | длительность бана | 168ч (7 дней) |
| Name Pattern | действие | ban |
| Name Pattern | длительность бана | 0 (навсегда) |
| Mass Join | окно | 60 сек |
| Mass Join | порог | 10 вступлений |
| Mass Join | действие | slowmode |
| Mass Join | slowmode | 60 сек |
| Mass Join | авто-снятие | 30 мин |
| Mass Invite | окно | 300 сек (5 мин) |
| Mass Invite | порог | 5 инвайтов |
| Mass Invite | действие | warn |
| Mass Reaction | окно | 60 сек |
| Mass Reaction | порог (юзер) | 10 реакций |
| Mass Reaction | порог (сообщение) | 20 реакций |
| Mass Reaction | действие | mute |

### Миграция

```bash
# Создание таблиц
alembic upgrade e1f2g3h4i5j6
```

Миграция создаёт:
- Таблицу `antiraid_settings` (per-group настройки)
- Таблицу `antiraid_name_patterns` (паттерны имён для бана)
- Индексы для быстрого поиска

### Интеграция в captcha_coordinator

Проверка имени по паттернам интегрирована в `captcha_coordinator.py`:
- **chat_join_request:** Проверка ДО капчи → отклонение + бан
- **new_chat_members:** Проверка ДО капчи → бан

Логика:
```
Пользователь входит в группу
    │
    ├── check_name_against_patterns(session, user, chat_id)
    │   ├── get_full_name() → "Д.е́. t.С.k.ő.ē TKL98ВОТ"
    │   ├── normalize_name() → "детское" (TextNormalizer)
    │   └── check_pattern_match() → matched=True
    │
    └── Если matched:
        ├── apply_name_pattern_action() → ban/kick
        ├── send_name_pattern_journal() → уведомление в журнал
        └── return (прерывание обработки)
```

### Callback Data формат журнала
```
ar:unban:{chat_id}:{user_id}    # Разбанить
ar:ok:{chat_id}:{user_id}       # Удалить сообщение
ar:permban:{chat_id}:{user_id}  # Забанить навсегда
ar:unmute:{chat_id}:{user_id}   # Размутить
ar:unslowmode:{chat_id}:0       # Снять slowmode
ar:lock:{chat_id}:0             # Закрыть группу
```

### Статус реализации

- [x] Модели БД (Фаза 1)
- [x] Миграция (Фаза 1)
- [x] Settings service (Фаза 1)
- [x] Name Pattern Checker (Фаза 2)
- [x] Action Service (Фаза 2)
- [x] Journal Service (Фаза 2)
- [x] Journal Callbacks (Фаза 2)
- [x] Интеграция в captcha_coordinator (Фаза 2)
- [x] Join/Exit Tracker (Фаза 3)
- [x] Join/Exit Handler для выходов (Фаза 3)
- [x] Интеграция track_join_event в coordinator (Фаза 3)
- [ ] Mass Join Tracker (Фаза 4)
- [ ] Mass Invite Tracker (Фаза 5)
- [ ] Mass Reaction Tracker (Фаза 6)
- [ ] UI настроек (Фаза 7)
- [ ] Тесты (Фаза 8)

---

*Последнее обновление: 2026-01-17 (Фазы 1-3 Anti-Raid завершены)*